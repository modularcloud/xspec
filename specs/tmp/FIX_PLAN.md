# FIX_PLAN — Phase 9 (test harness), second re-descent, compliance determination 1

Planned 2026-09-10 from: reviewer A (TEST-SPEC.md §0–8, 28 gaps), reviewer B (§9–16, 31 gaps), reviewer C (§17–18 + CERTIFICATIONS.md, 4 gaps), and the VERIFY report at 8342cdc (three red self-tests in `test/self/certification-document.test.ts`; T4.5-8, T6.5-7, T6.5-9 diagnosed product failures — allowed at this phase, no task). Governing IP: `specs/patches/0001-external-ui-apis.md` (Stage: Tested). Bundle: `specs/SPEC.md`, `specs/TEST-SPEC.md`, `specs/CERTIFICATIONS.md`, `specs/IMPLEMENTATION.md`.

## Rules for every task (read before starting any of them)

- **Phase 9 scope guard.** Edit only under `test/`, `.github/`, harness configuration, `AGENTS.md` (build/lint/run knowledge only), and this file. Never touch `src/`. Product tests are expected to fail against the product; harness self-tests and certifications must pass.
- **Definition of done for a task:** `npm run typecheck` clean; `npm run format:check` clean (use the repository's format script to fix); `npm run test:self` fully green (certification included — any task touching a test named in a CERTIFICATIONS.md in-scope list re-certifies that fixture); the touched product-facing tests run against the built product (`npm run build` first, then `npx vitest run --config test/vitest.config.ts --project suite test/suite/<section file>.test.ts --reporter=verbose`) either pass or fail as a *diagnosed product failure* (a `HarnessAssertionError` whose message states what the product did wrong, TEST-SPEC H-8) — never a harness error, timeout, or crash. Commit `sdg(phase-9): <imperative summary>` ending with the two trailer lines the spawn prompt gives, push to `origin claude/xspec-ui-apis-4df8fa` (retry on network errors with 2s/4s/8s/16s backoff), then delete the finished task from this file (or the whole file when it is the last task).
- **Registration mechanics** (new test IDs): the body is a `ProductTestEntry` (`{ id, title, run }`) in the section's module under `test/suite/registry/section-*.ts`, spread into `test/suite/registry/index.ts`, mapped to its SPEC.md passages in `test/suite/registry/traceability.ts` (H-7; checked by `test/self/s1-traceability.test.ts`), and declared where the thin wrapper `test/suite/section-*.test.ts` / `test/suite/declare.ts` requires. Mirror the file set of commit 8342cdc (T6.3-5) or 24c9487 (T4.5-8): `git show --stat <sha>`.
- **Escape spellings.** The tool-parameter layer decodes `\uXXXX` in edit payloads; when a test needs the six-character source spelling (e.g. `id="a\u002Eb"`), write it as `\u002E` in the payload and verify the file bytes with `grep -c 'u002E'` afterwards.
- **Platform-gated arms.** Linux-only stagings (non-UTF-8 path bytes; permission removal) follow the existing pattern `const NU3_STAGED = process.platform === "linux"` in `test/suite/registry/section-11.5.ts`; a Linux-leg test (T13.5-7, T14-9, T14-10) must not be selected by the Windows project's registry-ID subset (E-6) unless its body is platform-safe — check the selection list before registering.
- **H-11 harness errors vs H-8 product failures.** `HarnessAssertionError` (`test/helpers/assertions.ts`) means "diagnosed product failure". An ineffective staging, an unreachable precondition, or a harness bug must surface as a distinct error class (`HarnessStagingError` in `test/helpers/permissions.ts`), never as a `HarnessAssertionError`, a pass, or a skip (H-9).
- **Infrastructure caution.** Keep each spawn's edits bounded: a few tool calls per response, modest payloads, no single giant write. Large section files (section-6.5.ts, section-13.5.ts, section-14.ts, section-12.7.ts) are edited with targeted `Edit`s, not rewrites.

Ordering: Part A turns the three red self-tests green (certification manifest/gate cluster). Part B lands shared helpers. Parts C–E are consumers, ordered by dependency; within a part, order is free. Parts A and B are complete: the CONF-VALID conformer (`test/fixtures/conf-valid/product.mjs`) now rejects `"`, `'`, `\`, `&`, and U+FFFD in segments and tags (14.4), reads attribute values verbatim, emits one 14.4 finding per offending `id`/`tags` attribute located at the attribute's own characters (name through closing quote), and emits tag sets in UTF-8 byte order with duplicates collapsed; P-1's oracle (`valueVerdict` in `test/suite/registry/section-16-p1.ts`) judges the same five characters invalid, since its alphabet already staged both quote characters.

Task 8 landed (459ea19, ca381ee): the harness vocabulary (`test/helpers/adapters/model.ts`) holds SPEC 14's 25 condition tokens — `USAGE_ERROR_CONDITION_CODE_TOKENS` names 14.24 `write-failure` and 14.25 `read-failure`, which the findings-array decode rejects and only the exit-2 error document's `code` admits — and no `refused-unresolvable-reference`. T14-6's 14.24/14.25 arms stage T14-9 (f) (`.xspec` unwritable on a stale workspace, `stageWriteRefusalUnder`) and T14-10 (g) (`specs/sub` unlistable, `stageReadRefusalOfDirectory`) through `test/helpers/permissions.ts`, Linux-gated by the NU3_STAGED pattern; a discovered source's refused content read is condition 20 (SPEC 14.25, T14-10 (a)), never `read-failure`, so Tasks 10–11 must not stage it as one. Against the built product both refusals end in exit 70 (an internal error at the `EACCES`), so T14-6 fails as a diagnosed product failure at its 14.24 arm and T14-9/T14-10 will diagnose the same.

Task 9 landed: T13.5-7 is rewritten as "interrupted or write-refused mutation: the pinned write order" — the refusal arms (a)–(f), their fixtures, stagings, twins, and pinned-state laws live in `test/suite/registry/write-refusal-staging.ts` (section-13.5.ts composes them and re-exports the hold helpers `holdPathFor`/`awaitHoldFile`, which moved there). For Task 10's T14-9 reuse: `RENAME_FIXTURE`/`MOVE_FIXTURE` (each with a prior journaled rename that makes the workspace journal-bearing — `build` creates no journal), `prepareRefusalWorkspace` (build, prior rename, `check` clean, git commit as the `impact` baseline), `completeOnTwin`, `runHeldWithStaging`/`runStaged` (staging applied at the seam, restored at exit), `refusalAt`/`refusalUnder` (the T14-9 disciplines), `expectWriteFailure` (exit 2, the error document, `code` `write-failure`, `path` among the admissible concerned paths), and the exported arms. The move fixture keeps the imported spec file under a second glob root (`docs/Other.mdx`) so `out/specs` holds exactly the two Markdown files (c) admits — SPEC 12.1 makes a regeneration rewrite every derived file, so a third `.mdx` under `specs/` would put a third refused write there. Red-checked with a stand-in that maps the product's exit-70 `EACCES` crash to the error document (AGENTS.md): every arm passes against the product's real partial states except (d), where the product rewrites the importer `src/app.ts` before removing the origin — a diagnosed product deviation from 13.5's order (the relocation's two writes precede `src/` in `files` order). Against the built product T13.5-7 fails diagnosed at arm (a)'s exit-code assertion (exit 70).

Task 10 landed: T14-9 is registered in the split module `test/suite/registry/section-14-ii.ts` (array `section14iiTests`, spread in `index.ts` after `section14ValidationTests`, declared by the wrapper `test/suite/section-14-ii.test.ts`, mapped in `traceability.ts` to "14", "12.0", "12.7", "13.3", "13.5") — the home for T14-10 too (Task 11: append it to `section14iiTests` in canonical order). Its arms are contract and recovery only (T13.5-7 asserts the per-command states): each stages through Task 9's helpers, decodes the error document with `expectWriteFailure` plus a non-empty-stderr check, and ends with `build` exit 0 and `check` clean. Arm (a) drives the child rename `b.k` → `b.k2` (referenced nowhere) so the refused rewrite of `specs/b/B.mdx` is the operation's first write and the recovery clause holds on the pinned "nothing written" state — the multi-file rename's partly applied, failing state being T13.5-7 (a)'s; (b)–(f) stage as T13.5-7 does, plus `specs/sub` unwritable for the destination's production (nothing relocated). The reporter sweep runs on one stale, session-bearing workspace of the rename fixture extended with a code group `app` (`src/app.ts` importing A's module): the nine refreshing reads and `review create` each concern `.xspec`; `check` exits 1 with 14.10 alone, `inventory` and `version` exit 0, `rename --preview` exits 0; `query nodes --group app` and `query node specs/a/A.mdx#missing` exit 2 with `code` null; a failing twin (A's `d` reference respelled) gives `ids` exit 1 with one 14.5 finding; the identity-unchanged rename under (a)'s staging exits 1 with `refused-identity-unchanged` alone and nothing written; a `--test-hold` path in a read-only directory beside the workspace exits 2 with `code` null. Through the AGENTS.md stand-in every arm passes against the product's real partial states (~40 s), and wrong-path and silent-stderr perturbations are caught at arm (a); against the built product T14-9 fails diagnosed at arm (a)'s exit-code assertion (exit 70).

Task 11 landed: T14-10 is registered beside T14-9 in `test/suite/registry/section-14-ii.ts` (`section14iiTests` = [T14_9, T14_10]; traceability "14", "10.7", "11.2", "11.5", "11.6", "12.0", "12.7", "13.3"), Linux-gated through `WRITE_REFUSALS_STAGED`, one arm per row of 14.25 through `stageReadRefusalOfFile` (mode 0o200) and `stageReadRefusalOfDirectory` (mode 0o100). Arm (a) uses its own fixture (`specs/A.mdx` → `specs/B.mdx` → `specs/C.mdx` ← `specs/D.mdx`, plus `src/app.ts` marking `A.a` under a code group) with a premise that B's `d` reference and the marker record occurrences when readable, so "no record for its spellings" is a masking, not an absence; (b)–(f) and (h) run on `RENAME_FIXTURE` via `prepareRefusalWorkspace`, (g) on the rename fixture plus `specs/sub/S.mdx` (and a second workspace with an invalid configuration for the 14.14-precedes-the-read clause). Snapshots for the nothing-modified laws are taken before staging and after restoring (a staged object cannot be snapshotted). Arm (f) stages every plain file of T13.3-2's graph-data set (`isGraphDataKey`, files only — directories keep their listing and write permission so the regeneration `ids` owes is never itself refused) and restores only the stagings whose files survive the regeneration; its preview is `move specs/c/C.mdx specs/c/D.mdx --preview --json`. The two unstageable clauses (a refused kind read; a directory above the root) are recorded in the module header and the title, asserted nowhere. Against the built product in the unprivileged namespace, each arm driven alone: (a), (c), (d), (e) pass; (b) and (g) die with exit 70 (`EACCES` at the journal `open` / the `scandir` of `specs/sub`); (f)'s `inventory` reads the unreadable record as the empty record (`"recorded": []`, findings [], exit 0); (h)'s `review list` answers `"sessions": []` at exit 0 — so T14-10 fails diagnosed at arm (b) within seconds, and no arm raises a harness error.

Task 12 landed: T12.7-3 (`test/suite/registry/section-12.7.ts`) gained, inside `runErrorConfigPathsArm`, the unoccupied-`--config` echo arms — from `work/`, `--config ./../cfg//xspec.config.ts` and an absolute `cfg/absent.config.ts`, each reported byte-for-byte as given (SPEC 14), the pair snapshot-compared as modifying nothing — and, once the malformed `cfg/xspec.config.ts` exists, the same two spellings reported canonical `../cfg/xspec.config.ts` (existence, not spelling, decides the form); plus two Linux-leg arms gated by `LINUX_LEG_STAGED` (the NU3_STAGED pattern, run last): `runErrorSymlinkWorkingDirectoryArm` (working directory `R/L`, a symlink to `R/a/b`; `--config ./../xspec.config.ts` naming the malformed `R/a/xspec.config.ts`; reported `../xspec.config.ts`) and `runErrorEnvironmentRefusalArms` (T14-6's stagings — `stageWriteRefusalUnder(.xspec)` on a stale built workspace, `stageReadRefusalOfDirectory(specs/sub)` — each `build --json` decoded by the new `assertEnvironmentRefusalDocument`: exit 2, non-empty stderr, form-exact `{"code": "write-failure", "path": ".xspec"}` / `{"code": "read-failure", "path": "specs/sub"}` with locations []). Traceability stays `["12.7"]` per the row's recorded precedent (14.24/14.25's primary tests are T14-9/T14-10, which T12.7-3 cites); T12.7-3 is in no fixture's in-scope list, so certification is untouched. Against the built product: the existing-file spellings and the symlinked working directory answer exactly as pinned, but the product canonicalizes an unoccupied `--config` value (`../cfg/xspec.config.ts`, `../cfg/absent.config.ts`) where 14 pins the argument as given, so T12.7-3 now fails diagnosed at that first new arm (a `HarnessAssertionError` showing actual vs expected path); the refusal arms would fail diagnosed at their exit-code assertion (exit 70). Behind the AGENTS.md stand-in (the unoccupied value echoed as given; exit-70 `EACCES` mapped to the error document) the whole test passes in ~6 s, and a wrong concerned path or an empty stderr on the refusal arms is caught at the write-failure arm. Self project 357 passed / 1 skipped under the unprivileged namespace.

Task 13 landed: T14-7 (`test/suite/registry/section-14.ts`) gained two stagings of its own. The destination-spelling workspace (`T14_7_SPELLING_FILES`: a root-level origin `a.mdx` under a root-level glob `*.mdx`, beside `specs/b.mdx`) drives `./a.mdx`, `specs//b.mdx`, and `specs/../specs/b.mdx` as the file form's destination and as a section form's target path (`<new-id>` `z`, colliding with nothing), each refused `refused-invalid-destination` alone — the exact one-entry multiset excludes `refused-destination-exists` and `refused-identity-unchanged` — with `path` the spelling as given. The import-cycle workspace (`specs/B.mdx` importing `A` and using the binding through `d={A.keep}`; `user` in `specs/A.mdx` referencing the moved `x` in local form) drives `move specs/A.mdx#x specs/B.mdx#x` and asserts `refused-cycle` every-participant strict through `locatedAtEach`: the local reference spelling in A (the `d={"x"}` window) then B's import declaration (the window of its own characters), path null — never a range for the import that does not yet exist. The no-unlisted-code clause is recorded at `assertRefusalReport` and in the module header rather than by new code: the form-exact decode admits only 14's codes (`forms.ts` KNOWN_CODE_TOKENS), so an unlisted code fails as an H-3 form failure before any count, and the exact multiset excludes every listed code beside the staged reasons. Against the built product the six spelling arms answer exactly as pinned, while the import-cycle arm fails diagnosed — the product locates B's import declaration `[0, 25)` alone, not the local reference spelling whose rewrite adds the closing import — and T14-7 as a whole still fails diagnosed first at its rename `refused-invalid-id` arm (the produced `a.then.kid` identity). Red-checked through the AGENTS.md stand-in with base fixes for the product's earlier deviations: the whole test passes in ~22 s, and the product's real cycle answer, an extra occupied-destination finding, an unlisted code (`refused-unresolvable-reference`), an extra would-be-import range, and a normalized `path` are each caught as a `HarnessAssertionError`. Traceability stays `["14"]` (T14-7's row). Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace, run alone.

Task 14 landed: T14-11 is registered in `test/suite/registry/section-14.ts` (spread through `section14ValidationTests`, so `index.ts` and the wrapper needed nothing; traceability "14", "1.7", "5.7", "11.2", "11.4"). Fixtures are assembled from parts (`assemble`/`pin`: every offset the UTF-8 byte length of the text before the pinned part, a multibyte `é` before every pinned construct), `build --json` is decoded form-exact, and `assertExactRanges` pins the exact condition multiset, `path` null, and each condition's complete location lists as a multiset of exact `{start, end}` ranges. Arms (a)–(m) are one `RangeRuleCase` each — a `d` array entry, `d={foo}`, `d={}`, `SPEC?.a;`, 14.2 (two bearers), 14.3, 14.4 (three attributes), 14.17 (four forms in one file), 14.1, 14.15 (five forms in one workspace plus 14.7 for the marker and the `text(...)` call the collision leaves unresolved), 14.16 (four forms), 14.18, 14.20 (BOM, `Café` + 0xFF, the unclosed section, `let x = ;`); (n) drives the repeated-`d` file through `build` and `occurrences` (exactly one record, the resolving entry's span); (o) is the refused read (`stageReadRefusalOfFile`), Linux-gated by `T14_11_REFUSAL_STAGED`, run last. Against the built product (the per-arm driver, AGENTS.md): (a), (b), (d), (e), (f), (g), (i), (k), (l), (o) pass; (c) fails diagnosed (`d={}` reported 14.20 at [46, 47)), as do (h) (the repeated prop located at the second `id` alone), (j) (the collision file reports nothing — T4.5-8's deviation), (m) (every 14.20 range non-zero-length: BOM [0, 3), encoding [5, 6), the unclosed section [0, 13), `let x = ;` [8, 9)), and (n) (no 14.5 for the unresolved entry, the 14.17 at the second `d` alone) — so the registered test fails diagnosed at arm (c) in ~1 s, never as a harness error. `d={}` is not well-formed MDX under the reference grammar (attribute value expressions reject the empty expression): recorded in `specs/tmp/SPEC-PROBLEMS.md` (2026-09-11); the arm is kept as TEST-SPEC pins it. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 15 landed: T13.5-1 (`test/suite/registry/section-13.5.ts`) gained, after its non-mutating unknown-flag arms, the `build --test-hold --json` arm — `--test-hold` being value-taking by name on every command (SPEC 12.0), `--json` is consumed as the hold path (a filesystem path resolved against the working directory, so `<root>/--json`) and JSON is out of effect: exit 2, stdout byte-empty, no hold file at `./--json`, the workspace unchanged (`assertLeavesUnchanged`); the title and module header record the arm. The CONF-CORE conformer needed no change: its `parseArgs` refuses `--test-hold` on `build` before any token is consumed and its usage errors print to stderr alone, so it passes the arm, and the certification's expected outcomes are unmoved (T13.5-1 fails on exactly VIOL-CORE-EARLYWRITE and VIOL-CORE-EARLYREFRESH, passes on the conformer and the other six). Against the built product T13.5-1 now fails diagnosed at this arm — the product reads `--json` as the JSON flag and answers the 12.7 error document on stdout (151 bytes, `code` null, `message` "build: unknown flag '--test-hold'") where 12.0 makes it the hold path — every earlier arm passing; a new diagnosed product failure beside the known ones. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 16 landed: T13.3-2 (`test/suite/registry/section-13.3.ts`) gained the absent-record clause on its deletion arm, and the arm itself was reshaped — its whole-workspace byte compare against the post-build fixed point W0 could hold only for a product whose refresh writes a fresh record (the record is part of the graph data the operational deletion removes, and a conforming refresh leaves it absent, SPEC 13.3, while every `build` writes one; the arm predated the clause, which entered SPEC.md at 0cc11e6 and TEST-SPEC.md at 3031926), so it now follows Arm B's shape: outside the graph data byte-identical to W0 (no TypeScript or Markdown generated or removed, durables untouched), graph data present after each read and byte-identical across the nine reads (12.0), and — inside one compare-around — `inventory` reporting `recorded` exactly `[]` (new local helper `assertEmptyRecord`: the value state, never unavailable or null) and `check --json` clean (`expectFindingFreeReport`) after every read, with a premise `inventory` on the deleted state before any read (`recorded` `[]`, modifying nothing). The module header records why the deletion arm admits no build reference and that `check` clean is the product's own 14.10 comparison standing in for "as `build` would write it"; the title carries the clause; traceability gained "11.6". Green through a stand-in that empties the record the product's refresh writes (AGENTS.md); against the built product T13.3-2 now fails diagnosed at the first post-read `inventory` in ~3 s (`refreshedGraphData(null, build)` writes `build`'s eight-path record where 13.3 pins the absent record) — a new diagnosed product failure beside the known ones, no harness error. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 17 landed: T14-2 (`test/suite/registry/section-14.ts`) gained the escape-spelled arms inside its one `build --json` sweep — counts now `{14.5: 2, 14.6: 1, 14.7: 3}` — beside the node `login` the interpreted spellings would name (T2.4-5's premise: added to `specs/base.mdx` in the initial state, so the prior valid generation exports it, and to `specs/ref.mdx` in the broken one): a fourth section `r3` with the local literal `d={"lo\u0067in"}` (14.5, located within the element's window) and a second consumer file `src/escaped.ts` holding the escape-free control `BASE.login` then the marker `BASE.lo\u0067in` (14.7, located within the statement's window, sorting after `src/app.ts`'s two in the pinned findings order), which a `ConsumerProject` rooted at that file type-checks clean (`assertNoCompileErrors`: TypeScript reads the escaped identifier as `login`, which the generated module exports — 14.7's type-error clause holds only for an escape-free spelling, SPEC 14.7, 2.4). A condition's findings are selected in (file, range start) order by the new local `findingsInSourceOrder` (`findingOf`'s exactly-one rule no longer applies to 14.5 and 14.7). Traceability gained "2.4" and "4.5"; T14-2 is in no fixture's in-scope list, so certification is untouched. Green through a stand-in that inserts the two owed findings after their originals (AGENTS.md); a wrong range on either is caught at its window. Against the built product T14-2 now fails diagnosed at the count assertion in ~1 s — it reports `14.5 x1, 14.7 x2`, resolving the interpreted spellings (`lo\u0067in` as `login`) where SPEC 2.4 reads them verbatim — a new diagnosed product failure beside the known ones, no harness error. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 18 landed: T1.4-1 and T1.4-4 (`test/suite/registry/section-1.4.ts`) stage the rewritten 1.4 alphabet — the quote, escape, and character-reference characters (`"` inside a single-quoted value, `'`, `\`, `&`) and U+FFFD, each as its raw character between two ordinary letters in a segment and in a tag — and the verbatim spellings of SPEC 2.4: the six-character escape of `.` and the reference `&#46;` in a segment, the escape of `y` in a tag, each asserted as exactly one 14.4 finding (a product interpreting them reads `a.b`, structurally invalid at the top level, or the accepted tag `xy`, and fails the arm). Every negative arm of both tests now pins its 14.4 finding exactly at the offending attribute's own characters — name through closing quote, the attribute range of SPEC 11.4 — through a parts builder `assemble` (each pinned part's offset the UTF-8 byte length of the text before it) and `assertOnly144AtAttributes` (one finding per offending attribute, one location each, ranges compared as a multiset); the nested empty-segment arm pins exactly two findings, one at `id="a."` and one at `id="a..b"` (CERTIFICATIONS.md §CONF-VALID: the descendant spelling the malformed ancestor segment as its own prefix reports in its own attribute too). The escape spellings are built from the backslash's code point (`String.fromCodePoint(0x5c)` followed by the plain text `u002E`), so no tool layer can decode them on the way into the file — the safe pattern for the escape spellings Tasks 21, 23, 27, 33, and 41 need. Certification unchanged in shape and green: CONF-VALID passes both tests, VIOL-VALID-CTRL fails exactly at their U+0000 arms, VIOL-VALID-WIDE fails T1.4-2 and T1.4-4's boundary arm with T1.4-1 passing. Against the built product both tests fail diagnosed at their first new arm (`"` in a single-quoted value builds, exit 0); a scratch probe shows the product accepting `'`, `\`, `&`, U+FFFD, and the escape-spelled `.` and `y` in segments and tags (exit 0) and interpreting `a&#46;b` as the two-segment `a.b` (`invalid-structural-id` at [46, 58)) where 2.4 reads the reference verbatim — new diagnosed product failures beside the known ones; every pre-existing arm, now with its exact attribute range, passes against the product. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 19 landed: P-1's alphabet (`test/suite/registry/section-16-p1.ts`) now draws `\`, `&`, and U+FFFD — each an invalid boundary class at weight 3, the quote characters' weight, grouped with them after the glob metacharacters (`AMPERSAND`, `BACKSLASH`, `REPLACEMENT_CHARACTER`, built through `cp(0x…)`): `\` and `&` are staged raw inside the quoted value, which SPEC 2.4 reads verbatim, so a draw holding one is predicted rejected on the character itself (14.4) and a product interpreting an escape or reference form answers for a value it was never given; U+FFFD is staged as the literal, validly encoded code point (EF BF BD), so 14.4 — never 14.20 — is at stake. The header's class list and its "still to land" bullet are rewritten; the both-quote redraw (`spellable`) is unchanged, TEST-SPEC P-1's "not staged" governing; the oracle needed nothing (Task 7). No self-test pins P-1's draw statistics. Measured through the AGENTS.md twin procedure on the fixed seed set (25 trials × 3 seeds per property): segments 20 accepted / 55 rejected with 4 CTRL-flip and 7 WIDE-flip draws (both unchanged from before the entries), `\` in 2 draws, `&` in 5, U+FFFD in 5 (one pure `\` draw and one pure `&` draw — the only 1.4 violation — among them), 7 accepted nested chains, one single-quoted spelling, no both-quote draw; tags 35 / 40 with 14 CTRL-flip and 5 WIDE-flip (14 and 4 before), `\` 5, `&` 5, U+FFFD 3 (three pure `&`, one pure U+FFFD), one single-quoted spelling. Certification unchanged in shape and green: P-1 passes CONF-VALID and fails exactly on VIOL-VALID-CTRL and VIOL-VALID-WIDE (CONF-VALID 12/12; each violator 9 pass / 3 fail, its expected three). Against the built product P-1 fails diagnosed in ~10 s at seed 271828183, trial 11 of the segment property: the initial falsifying draw `a\Ab` (a segment containing `\`) built with exit 0 where 1.4 rejects it, shrunk to the single-quoted `"` draw, also accepted — the known P-1 product failure, now reached through the escape character too; no harness error. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 20 landed: T1.5-2 (`test/suite/registry/section-1.5.ts`) and T11.5-3 (`test/suite/registry/section-11.5.ts`) stage the U+FFFD-pathed spec source on both legs through one shared spelling, `REPLACEMENT_CHARACTER_SPEC_PATH` (`specs/A<U+FFFD>.mdx`, built from the code point `0xfffd` in `test/suite/registry/support.ts`). That constant is the only sharing the plan's `stageNonUtf8Path` extraction would have added: the builder's `file(rel: RelPath, …)` already takes byte paths, S-2 round-trips them, and both tests already stage the 0xFF path through it, so `test/helpers/workspace.ts` gained nothing. T1.5-2's non-UTF-8 arm now pins its finding's projection (`invalid-source-path`, locations [], the marked byte form composed from the staging bytes) and a new both-legs arm pins the U+FFFD file's finding with the concerned path as the plain string, never the byte form. T11.5-3 stages the file as a fifth source (gate counts 14.19 x2 on Linux, x1 elsewhere; the 14.19 projections compared in one canonical order through `inCanonicalOrder`, though the adapter already enforces 12.7's pinned findings order), drives `at specs/A<U+FFFD>.mdx 0` through a local `expectPlainUsageError` (exit 2, the error document with `code` and `path` null, non-empty stderr) on the workspace and again on a configuration-less workspace holding the same file — SPEC 12.0's own discriminator: a syntax-class malformed value is reported without loading configuration, while the unknown-file check consults discovery and is preceded by 14.14 — with the two stdouts byte-identical (T12.0-10's discipline), and pins `view --file specs/A*.mdx` as the NU3 view is pinned (exit 1, the one finding with the plain-string path, `file` the plain string, the tree byte-exact with every identity unavailable). The NU3 spellings keep their exit-2 assertions, their prose now saying malformed value or unknown file (TEST-SPEC pins neither for that file). Traceability: both tests gained "12.0", "12.7", "14". Neither test is in a fixture's in-scope list, so certification is untouched. Against the built product both fail diagnosed within seconds — the product discovers the U+FFFD-pathed file as valid (`build --json` finding-free at exit 0, `view --file specs/A*.mdx` with defined identities at exit 0), so T1.5-2 fails at the new arm's exit code and T11.5-3 at its gate count (14.19 x1) — while the product already refuses the U+FFFD `at` spelling as a malformed value on both workspaces; new diagnosed product failures beside the known ones, no harness error. Green through a stand-in that appends the file's 14.19 finding in 12.7's order and marks the view's identities unavailable (AGENTS.md pattern); a configuration-error document on the configuration-less `at` is caught at the `code`-null check, a byte-form path at the H-3 decode. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 21 landed: T2.1-2 (`test/suite/registry/section-2.1.ts`) gained the lexical positives — one workspace holding `specs/BASE.mdx` and four importers `specs/P1.mdx` … `specs/P4.mdx` spelling it `./sub/../BASE.xspec` (no `specs/sub/` on disk), `.//BASE.xspec`, `././BASE.xspec`, and `../specs/BASE.xspec`: `build` exit 0, each `query edges --from specs/Pn.mdx#pn` exactly the one `depends` edge to `specs/BASE.mdx#core`, and one bare `view` (finding-free, the five sources in path order) whose per-importer `imports` member projects to exactly `[{ name: "BASE", target: "specs/BASE.mdx" }]` (T11.4-4's datum; ranges not pinned here) — and three invalid arms beside the existing five: an `.mdx` matched only by a code group (`SPECS_AND_DOCS_CODE_CONFIG`: code group `docs` over `docs/**/*.mdx`; `docs/EXTRA.mdx` holds `export {};` so the code source itself contributes no finding and the 14.15 stands alone), the above-root `../../outside/BASE.xspec` with a real `outside/BASE.mdx` at the root's parent, and the escape-spelled `./B<backslash>u0041SE.xspec` beside an existing `specs/BASE.mdx`. T4-2 (`test/suite/registry/section-4.ts`) gained the above-root arm (`../../outside/NAME.xspec`, a real `outside/NAME.mdx` beside the root), the escape-spelled name segment `./N<backslash>u0041ME.xspec` under a new `COLOCATED_CONFIG` (spec group `["specs/**/*.mdx", "src/**/*.mdx"]`, code group `src/**/*.ts`; the derived `src/NAME.xspec.ts` is excluded by 13.4) with `src/NAME.mdx` discovered beside the code file, and the lexical positives `src/one.ts` (`./sub/../NAME.xspec`, no `src/sub/`) and `src/two.ts` (`.//NAME.xspec`), each with a bare marker `NAME.core;` recording exactly its `references` edge to `src/NAME.mdx#core`. Both arm interfaces gained `outsideFiles` (paths relative to the root's parent, staged through the new `stageBesideRoot` in `support.ts` — under the workspace's `tempRoot`, disposed with it) and section-2.1.ts's `withWorkspace` a `config` parameter; the escape spellings are composed from `String.fromCodePoint(0x5c)` (Task 18's pattern; `grep -c 'u0041'` finds three composed occurrences per module, no decoded variant). Traceability: T2.1-2 `["2.1", "2.4", "11.4", "14"]`, T4-2 `["4", "2.1", "2.4", "14"]`; neither test is in a fixture's in-scope list (CERTIFICATIONS.md's uncertified negative matrices name T2.1-2's lexical and above-root arms), so certification is untouched. Against the built product both tests fail diagnosed at their escape-spelled arm — `build --json` exits 0, the product interpreting the escape and resolving `./BASE.xspec` / `./NAME.xspec` where SPEC 2.4 reads the specifier verbatim — every arm before it passing, and with that arm filtered out T4-2 passes whole (the above-root arm, whose hand staging shows the product answering `invalid-import` "resolves outside the workspace root" with the outside file present, and the lexical positives included); no harness error. Not covered by any plan task (the refinement's T4-2 additions, left to the compliance determination): the side-effect-only `import "./NAME.xspec"` valid in a code-group file (`build`/`check` exit 0, no edge, no occurrence) with `import "./missing.xspec"` and `import "../specs/NAME.xspec.ts"` each 14.15. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace, run alone.

Task 22 landed: T2.4-5 is registered in `test/suite/registry/section-2.4.ts` (SUITE-08 now T2.4-1 … T2.4-5; `index.ts` and the wrapper spread the array, so neither changed; traceability `["2.4", "1.4", "4.5", "5.7", "11.3", "14"]`). One workspace under `SPEC_AND_CODE_CONFIG`: `specs/BASE.mdx` holds `login` and `a`/`a.b`, the head of `specs/A.mdx` the local `login` and `a`/`a.b` (every negative staged beside the node its interpreted value would name), `src/app.ts` the escape-free control `BASE.login;` — the initial state builds (the prior valid generation the type-check compiles against), then the edit appends the five MDX arms as exact parts (`T2_4_5_MDX_PARTS`; each construct's window from the parts before it) and the escaped marker. `build --json` is pinned to exactly `{14.5: 3, 14.6: 2, 14.7: 1}`, each condition's findings selected in source order through `findingsInSourceOrder` (moved from section-14.ts to `support.ts`, 532f8ad) and located within its construct's window; the consumer file type-checks clean (`assertNoCompileErrors`); bare `occurrences` (exit 1, the same findings accompanying) is pinned to exactly the control's record — the (file, kind, source, target) multiset through section-5.7.ts's now-exported `renderOccurrenceUnit`/`expectedUnitMultiset`, plus the exact span of the bare chain `BASE.login` — since `query edges` refuses to answer on a failing workspace (13.3), the occurrence record is the edge's witness. Green through a stand-in (AGENTS.md) that clones the product's `unknown-dependency` finding into the four owed findings and filters `occurrences` to the control's span; a kept record for the escaped marker is caught by the multiset, a mislocated finding by its window. Against the built product T2.4-5 fails diagnosed at the count assertion in ~0.6 s — it reports `14.5 x2` (`"a&#46;b"` read verbatim, correctly; `BASE["a\u002Eb"]` interpreted to the single segment `a.b`) and resolves `d={"lo\u0067in"}`, `{text("lo\u0067in")}`, `text(BASE["\u0061"]["b"])`, and the marker `BASE.lo\u0067in` to the nodes their interpreted spellings name, recording an occurrence for each — the known 2.4 deviation. Two notes for the compliance determination, neither a defect: TEST-SPEC's "though the interpreted computed keys would" holds for `text(BASE["\u0061"]["b"])` but not for `BASE["a\u002Eb"]`, whose interpreted key is the single segment `a.b` (14.5 under T2.4-4's rule either way; the arm is kept as pinned); and the tool-parameter layer decoded `\uXXXX` spellings inside the comment text of a Bash heredoc too, not only in edit payloads — the module's comment spellings were rewritten from the code point afterward (`grep -c 'u0067\|u002E\|u0061'` on section-2.4.ts finds ten lines). Self project 357 passed / 1 skipped under the unprivileged namespace.

Task 24 landed: T2.7-1 (`test/suite/registry/section-2.7.ts`) gained the section-in-container arm per the *current* TEST-SPEC text — the plan's `ids`/`query` check was stale: both are gated on the failing workspace (SPEC 13.3), so the absence is asserted in the view's tree alone. One workspace holds the module's sibling template, then `<S id="sec">` whose body spells `{<S id="x">Inner x text.</S>}` after one multibyte character (`é`), so every pinned offset is a UTF-8 byte length: `build --json` is pinned to exactly one finding, 14.16, located exactly at the container from its opening brace through its closing brace ([74, 103) of the 121-byte file, `path` null), and the bare `view --text` (exit 1, the same finding accompanying, one view) is pinned by a targeted walk (no node whose range starts within the container's range — the sharp diagnosis) and by the exact projected text tree (identity, range, ownText, subtreeText, children; T11.2-4's projection): root [0, 121) with own text `"\n"`, `ok` [0, 41), `sec` [43, 120) with the container's bytes preserved byte-for-byte in its own and subtree text, the tag lines dropped, the root's subtree text in document order; imports, occurrences, and comments each `[]`. Traceability gained "11.2" and "11.4"; no fixture scopes T2.7-1, so certification is untouched. Against the built product the arm passes — the product answers exactly as pinned — so T2.7-1 stays green; a stand-in (the AGENTS.md wrapper pattern) adding a node `x` inside the container, shifting the finding's end by one, or stripping the container's bytes from the texts is caught as a diagnosed failure at the respective assertion. Not registered this iteration (no plan task covers them; for the compliance determination after the plan): the revised T2.7-1's fragment arm (`<>…</>` — one 14.16 from `<>` through `</>`, no node, enclosed content preserved under `view --text`) and its attribute-value-expression arms (`<S id="x" d={1}>` reports 14.8 alone; `<div a={1}></div>` exactly one 14.16), and T2.7-4, which is registered nowhere. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 25b landed (d9fe529): T3-3 (`test/suite/registry/section-3.ts`) stages its ESM-block arm as a second ESM block after the multi-line-tag arms — a blank line before it (MDX 3's ESM construct interrupts no paragraph) and after it (the block runs to a blank line) — `import B2 from "./BASE2.xspec"` U+2028 `import B3 from "./BASE3.xspec"` (`ESM_BLOCK_LINE`; the two new base files `specs/BASE2.mdx` and `specs/BASE3.mdx` staged beside `BASE.mdx`, the bindings unused), compiled to a kept line holding U+2028 alone plus its terminator, byte-asserted; the whole `DROP_SOURCE` derives under the stock MDX 3 parser as two `mdxjsEsm` nodes, the second holding two ImportDeclarations (AGENTS.md's recipe), and `K19 final` became `K19 after-inline-kept` with `K20 final` closing the fixture. The CONF-MD conformer's lexer (`test/fixtures/conf-md/product.mjs`) recognizes an import at a Markdown line start or, through `continuesEsmBlock`, continuing the open ESM block after ECMAScript WhiteSpace/LineTerminator code points alone (U+2028/U+2029 beside LF/CR; U+00A0, U+FEFF, and the Space_Separators beside space and tab; the next declaration admitted after a LineTerminator or a spelled `;`), the separator staying content; any other character closes the block, so content after a block stays content. Hand-driven on a minimal workspace: the conformer emits `K`, blank, U+2028, blank, the content line; VIOL-MD-CLASS drops the U+2028 line; VIOL-MD-CR emits as the conformer. Certification outcomes unmoved (T3-3 fails exactly VIOL-MD-CLASS and passes CONF-MD and VIOL-MD-CR); against the built product T3-3 still passes — the product keeps the line. The conformer header's stale example of an import directly followed by a non-blank line (T3-3's, restaged at 97cbc99) is corrected — the header now cites T3-1's `gamma` alone; whether other committed section-3 shapes derive is S-9's matter for the compliance determination. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 26 landed: T4.4-1 (`test/suite/registry/section-4.3-4.4.ts`) is rebuilt on the restated entry, its two spec modules now at the paths TEST-SPEC pins verbatim (`specs/A.mdx` node `a`, `specs/B.mdx` node `b`; shared with T4.4-2, whose sources and byte expectations moved with them; the ALPHAMOD/BRAVOMOD stems and the stem-containment operationalization are gone, section-12.7.ts's Arm C comments now calling that T4.4-1's former operationalization). The home-context arm pins `build --json` and `check --json` (the latter counted over the non-14.10 findings, the T11.2-6 precedent) to exactly one 14.11 finding located at the call alone, callee through closing parenthesis (`callRange`), `path` null, `identities` exactly `["specs/B.mdx"]` (`assertCrossModuleFinding`), and `occurrences` on the failing workspace (exit 1, the finding accompanying) to exactly one record projected whole — `embeds`, the call's span, source the whole-file location `src/app.ts` with the file's own range (SPEC 4.6, 1.7), target `specs/A.mdx#a`; the resolving-argument arms pin `textB(A.missing)` to 14.7 alone and `textB(A.a!)` to 14.8 alone, each located within its statement, no record for either; the invalid-called-path arm (`specs/B#.mdx`, run on every platform per section-11.2.ts's `#` precedent — "Linux leg" names only non-UTF-8 stagings) pins 14.19 concerning that path beside the 14.11 whose `identities` are exactly `[]`, the record still listed; the consumer facet keeps the type error at the reference and pins the thrown value's string `message` to contain `specs/A.mdx` and `specs/B.mdx` as exact substrings (a thrown value without a string `message` diagnosed as such). Traceability gained "1.5", "5.7", "11.2", "11.3", "12.7"; T4.4-1 is in no fixture's in-scope list (CERTIFICATIONS.md Exclusions, Section 4), so certification is untouched. Green through a scratchpad stand-in (AGENTS.md's wrapper pattern) that re-codes the product's 14.11 on the two resolving-argument arms, empties an identity over the `#` path, and inserts the owed occurrence record from the finding's own location — every facet passes through it, the runtime message included, so the product's generated modules already throw a message naming both source paths. Against the built product T4.4-1 fails diagnosed at the home-context arm's `occurrences` assertion in ~2 s — no record for the cross-module call, where SPEC 5.7/14.11 make its edge and occurrence stand — with two further deviations behind it (`textB(A.missing)` reported as 14.11 alone, not 14.7; `identities` `["specs/B#.mdx"]` over the invalid path where 1.5 pins `[]`); its `build`/`check` finding, location, identities on the valid path, and 14.19 all answer as pinned. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

---

Task 27 landed: T4.6-3 (`test/suite/registry/section-4.6.ts`) is extended on the restated entry. Its first workspace (`SPEC_AND_CODE_CONFIG`) gained six code files beside `src/app.ts`, every marker targeting a dedicated section of `specs/N.mdx` so the complete `references` set (21 edges) pins each attribution: `src/wrap.ts` (the parenthesized, `as`-cast, `satisfies`-qualified, and non-null-asserted initializers `w1` … `w4` → the file; `w5` inside `wrapHost` → `src/wrap.ts#wrapHost`), the four default-export files (`src/dobj.ts`, an object literal whose property arrow holds the marker → the file; `src/dident.ts`, `export default impl` → `src/dident.ts#impl`; `src/dcall.ts`, an arrow argument of the exported call → the file; `src/dlit.ts`, `export default 42` beside a top-level marker → the file), `src/decl.ts` (`#f` behind two overload signatures, `#M.m` behind two method overloads, `#C.v` in the second of two sibling-block classes `C` whose first is abstract with `abstract get v(): number`, `#Amb.m` in a block-scoped class behind a top-level `declare class Amb` with a body-less `m` — never an `@N` form), and `src/esc.ts` (`function f` + backslash-`u006Fo`, composed from the code point and byte-checked → the file, never `#foo`); the unfiltered endpoint sweep (`namesForbiddenUnit`) rejects `#s`/`#t`, `#w1` … `#w5`, `#default`/`#run`, `#foo` and the escape spelling itself, and any `@N` chain of `src/decl.ts`. A second workspace under a three-glob code group (`src/**/*.ts`, `src/**/*.mts`, `src/**/*.cts`) stages `x.d.ts`, `x.d.mts`, `x.d.cts`, `x.d.css.ts`, and the control `x.dts.ts`, each a body-bearing `function f` around a marker plus a top-level marker (`specs/D.mdx`, two sections per file): `build` exit 0, `check --json` finding-free (`expectFindingFreeReport`), the complete `references` set (both markers of each declaration file sourced at the whole file, the control's inner marker at `src/x.dts.ts#f`), and a sweep for any declaration-file unit. The occurrence's whole-file `source` range is left to T1.7-2, whose declaration-file arm is registered nowhere (for the compliance determination). Traceability gained "2.4", "12.2", "14"; T4.6-3 is in no fixture's in-scope list, so certification is untouched. Against the built product T4.6-3 fails diagnosed at the first workspace's `references` set equality in ~0.5 s — the only difference is `src/esc.ts#foo` (the product interprets the escape-spelled name, the known 2.4 deviation); every wrapper, default-export, overload, abstract-getter, and `declare` arm answers exactly as pinned. Hand-driven, the declaration-file workspace builds and checks clean but the product attributes each `.d.*` file's inner marker to `path#f` — a second diagnosed deviation the arm catches: through the AGENTS.md stand-in (`esc` mode re-sourcing `src/esc.ts#foo` at the file) the test fails diagnosed at exactly that set equality, and in `conform` mode (also re-sourcing a declaration file's `#f` at the file) the whole test passes in ~2 s — no harness error in any mode. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 28 landed (ac55a70): T5.7-4 (`test/suite/registry/section-5.7.ts`) stages the restated entry's collision and cross-module classes in a second workspace of its own (`assertCollisionClassesRecordNothing`, placed after the exported `NO_OCC_*` table T11.3-1 expands by position, which is untouched): `src/collide.ts` (T4.5-8's class — `const SPEC = 1` beside `import SPEC, { text }`: 14.15 locating both declarations, 14.7 for `SPEC.a` and `text(SPEC.b)`, no record), `src/typed.ts` (T4-5's class — the value import beside `import type SPEC from "../specs/B.xspec"`: 14.15 over both imports, 14.7 for each chain, no record), `src/calltext.ts` (T4.5-9's class — `function text(x: unknown) {}` beside the import: 14.15, 14.18 at `SPEC.a`, no record), `src/cross.ts` (T4.4-1's class — `textB(A.missing)` 14.7 alone and `textB(A.a!)` 14.8 alone, no record for either; `textB(A.a)` 14.11 with `identities` exactly `["specs/B.mdx"]` and its `embeds` occurrence recorded beside it), and the control `src/ctrl.ts` recording its marker and call. Each code file is exact parts (`partWindow`); `build --json` pins the multiset 14.7 x5, 14.8 x1, 14.11 x1, 14.15 x3, 14.18 x1 with every finding located within its construct's window (a 14.15 one location per colliding declaration, `assertFindingLocatesExactly`), and a bare `occurrences` (exit 1, the findings accompanying) reports exactly the three resolving spellings' records. Traceability: `["5.7", "2.4", "4", "4.4", "4.5", "11.2", "14"]`. Against the built product the test fails diagnosed at the collision premise — the product reports 14.8 x1, 14.11 x2, 14.15 x1: no collision for the value-level `const` or the `function text`, the doubly-bound and colliding-`text` chains recorded as occurrences, the type-only-collided chains silently dropped, `textB(A.missing)` classed 14.11 rather than 14.7 alone, and no record for the resolving cross-module call — allowed at this phase; the first workspace still passes, T5.7-1 through T5.7-3 pass, `npm run test:self` green (no certification names T5.7-4).
Task 29 landed (012859c): T6.5-1 (`test/suite/registry/section-6.5.ts`) is rebuilt on the restated entry as four arms of one parametrized fixture at the paths TEST-SPEC spells — the moved `A.mdx` (three nested sections; its own import of `C.mdx` single-quoted where an arm rewrites it), the spec importer `I.mdx`, and T6.2-2's importing `.ts` file (`src/app.ts`, a marker and a `text(...)` call) — through `fileMoveArm`/`runFileMoveArm`: (a) `specs/A.mdx` → `specs/sub/A.mdx` (`"./A.xspec"` → `"./sub/A.xspec"`; `'./C.xspec'` → `'../C.xspec'`, single quotes kept; `"../specs/A.xspec"` → `"../specs/sub/A.xspec"`), (b) `specs/sub/A.mdx` → `specs/A.mdx` (an ascent lost: `'../C.xspec'` → `'./C.xspec'`), (c) `specs/w/A.mdx` → `specs/x/A.mdx` (the importer in `specs/y/`: `"../w/A.xspec"` → `"../x/A.xspec"`; the moved file's own `'./C.xspec'` → `'../w/C.xspec'`), (d) `specs/A.mdx` → `specs/A2.mdx` with the moved file's own `./C.xspec` and `.//C.xspec` (two bindings of `C`, both used) neither rewritten nor reported and no `.ts` importer, so the preview's `files` is exactly the relocation entry and the importer's `import-specifier-rewrite`. Per arm the preview on a copy is pinned form-exact (`findings` `[]`, `mapping` every node of the moved file root first, `files` through `assertPreviewFiles`: the `file-relocation` spanning the entire moved file under its pre-operation path, then the file's own specifier rewrite, each importer's one rewrite, entries in path byte order), the real move's performed-operation document carries the same mapping, `assertSpecifierRewriteByteContract` now takes `{range, literal}` pairs and compares each range's post-move bytes to the expected literal byte for byte (the kept delimiters around the canonical spelling; the diagnosis prints the found literal through `literalAt`), (d)'s moved file and every untouched file are byte-identical, the journal holds one entry, `check` exits 0, node and edge inventories are as pinned, and the T6.4-7 fresh-build protocol runs on arm (a). Traceability is now `["6.5", "6.6", "6.4", "2.1", "12.7"]`; the module header's "resolved rather than compared" note is rewritten; T6.5-1 is in no fixture's in-scope list, so certification is untouched (self-tests 357 passed, 1 skipped). Against the built product T6.5-1 passes (all four arms, 10.9 s): its rewrites are canonical with the quote style kept, and the same-directory relocation leaves the non-canonical `.//C.xspec` untouched and unreported; a flipped expectation fails as a diagnosed HarnessAssertionError naming the range, the expected literal, and the found `'../C.xspec'`. AGENTS.md records the `-t '<ID> '` single-test filter (the trailing space keeps T6.5-1 apart from T6.5-10 through T6.5-19).

Task 30 landed (59e247e): T6.5-6 (`test/suite/registry/section-6.5.ts`) is rebuilt on the restated entry's kept-ID arm — the moved subtree `x` holds `x.c` (`id='x.c'`, single-quoted so a normalizing re-emission of the unchanged attribute is caught at the byte compare) and `x.u` with `d={"x.c"}`; `move specs/A.mdx#x specs/B.mdx#x --preview --json` is decoded form-exact and pinned through `assertKeptIdMovePlan` — first the sharp no-op check (no `id-rewrite`, no `reference-rewrite` anywhere in the plan), then `files` exactly `specs/A.mdx`'s one `origin-deletion` ([29, 124): the construct's own characters plus the dropped merged line's terminator) and `specs/B.mdx`'s one zero-length `target-insertion` at 26 — and its `mapping` the three kept IDs under the new file part; the real move's applied mapping is the same, `specs/B.mdx` lands as its source + the moved text + U+000A and `specs/A.mdx` as its head alone (`assertFileBytes`, composed from 6.5 and 3), `check` is clean, and the identities (now seven), journal, exact-self-move, and collision arms follow unchanged. The module comment carries the third unstageable clause (the mirrored structural-parent check, both forms). Traceability `["6.5", "6.6", "12.7", "14"]`; T6.5-6 is in no in-scope list, so certification is untouched. Against the built product T6.5-6 fails diagnosed in ~0.6 s at the preview arm — three `id-rewrite`s and one `reference-rewrite` reported inside the origin deletion, the bytes exact — a new diagnosed product failure beside the known ones; the AGENTS.md stand-in (`conform`: strip the two classes from the preview) takes the whole test green in ~2 s. Self project 357 passed / 1 skipped (19 files) under the unprivileged namespace.
Task 31 landed (77eca11): T6.5-11 is registered in the new module `test/suite/registry/section-6.5-ii.ts` (`section65iiTests`, spread in `index.ts` after `section65Tests`, declared by the wrapper `test/suite/section-6.5-ii.test.ts`, mapped in `traceability.ts` to "6.5", "4.3", "4.5", "4.6", "5.7", "6.6", "12.7"), on the current TEST-SPEC entry: four arms of one fixture (`specs/origin.mdx` holding `x` then the unmoved `w`, `specs/target.mdx` holding `z`, `src/c.ts` opening with `import O, { text as t } from "../specs/origin.xspec";` and calling `t(O.x)` inside `f`) — (a) both bindings losing their last use, (b) an existing `import T from "../specs/target.xspec";` used by the marker `T.z`, (c) a second call `t(O.w)` in `g` keeping the origin declaration, (d) `export type N = typeof O.x;` remaining. Each arm: premise `build` and a clean standard-tooling compile, the move exit 0, the fresh identifiers read off the one rewritten call `<callee>(<root>.y)` (the root pinned to the existing `T` in (b); a callee or root equal to a retained binding — `T` in (b), `O`/`t` in (c) — diagnosed), `src/c.ts` composed without the added import (the call's span replaced, the origin line removed or kept) and the single inserted run isolated by the new `assertExactDeclarationInsertion` in `test/helpers/import-insertion.ts` (the byte-exact declaration — `import <X>, { text as <Y> } from "../specs/target.xspec"`, `{ text }` where `<Y>` is `text`, `import { text as <Y> } …` in (b) — followed by U+000A, every admissible reading returned, at least one at a line start required), origin and target bytes composed whole, `check --json` and `build --json` finding-free, the complete `embeds` and `references` sets, the compile clean or, in (d), failing with an error inside the type alias statement; (a)'s preview parity runs on a second identical workspace after the real move: `src/c.ts`'s entry exactly `import-removal` [0, 54), `reference-rewrite` [94, 100), and one zero-length `import-addition` whose offset, composed under 6.5 (the removal's start and end admitted alike, a strictly-inside offset diagnosed), is where the real move inserted. (b), (c), (d) pin no preview and no arm pins the origin's or target's plan entries (T6.6-4's business). Self-tests for the reader are in `test/self/import-insertion.test.ts`; T6.5-11 is in no in-scope list (CERTIFICATIONS.md Exclusions), so certification is untouched. Green through a scratch stand-in that performs the move itself and patches the preview (AGENTS.md), in both the `{ text as txt }` and `{ text }` spellings; nine perturbations (a `;`-joined or single-quoted declaration, a kept origin import, a mid-line insertion, a preview offset off by one line, a preview lacking the removal, a fresh identifier equal to a retained binding, a second default binding in (b)) each caught at their own assertion. Against the built product T6.5-11 fails diagnosed in ~2 s at (a)'s move exit code: the product rewrites the argument through the new default binding but keeps the callee `t`, then refuses its own would-be text as the cross-module call of 14.11 (exit 1, one `cross-module-text` finding located in the would-be text) where 6.5 rewrites the call whole; its preview plans an `import-addition` at 54 and the `reference-rewrite` but no `import-removal` for the origin declaration — a new diagnosed product failure beside the known ones, no harness error. Self project 360 passed / 1 skipped (19 files) under the unprivileged namespace.


Task 32 landed (2e7ac5e): T7-1 (`test/suite/registry/section-7-basics.ts`) gained the occupancy arms on the current TEST-SPEC entry, in a third workspace of its own — a valid root configuration discovering `specs/A.mdx`, `dirocc/xspec.config.ts` an empty directory, `linkocc/xspec.config.ts` a symbolic link to `../xspec.config.ts` (the workspace builder's `dirs`/`symlinks`; a kind self-check throws a plain harness `Error`, never a diagnosed failure, if either staging is not what it claims). "Every command but `version`" is `EVERY_LOADING_COMMAND`: the complete SPEC 12 command set with `review` and `query` by every subcommand (26 invocations), each spelled syntactically complete so no syntax-class usage error (12.0, reported without loading configuration) can precede the configuration load; `sweepOccupiedConfiguration` drives the set four times — from `dirocc` and `linkocc` by the upward search (concerned path exactly `xspec.config.ts`, never `../xspec.config.ts`, never the link's target) and from the root through `--config dirocc/xspec.config.ts` / `--config linkocc/xspec.config.ts` (the path as given, already canonical, so T12.7-3's two conventions coincide) — each through the shared `expectConfigurationError` plus the `{code, path, locations: []}` pin; `version --json` answers exit 0 from both working directories (`decodeVersionDocument`); one whole-root compare brackets the sweeps; the premise (`ids --json` from the root listing `specs/A.mdx` `a`) is driven last so the sweeps observe a tree with no graph data. `timeoutMs` 240_000 (the sweeps cost ~30 s against a real product at ~0.28 s per run); traceability now `["7", "14", "12.6"]`; T7-1 is in no fixture's in-scope list, so certification is untouched (self project 360 passed / 1 skipped). Green-checked against a conforming scratch stand-in and red-checked in five deviation modes (the file above, reading through the link, a located finding, a null path, a silent stderr), each caught at its own assertion. Against the built product T7-1 fails diagnosed at the first sweep: from `dirocc` the product skips the directory occupant, loads the root configuration, and exits 0 (`{"findings": []}`); it also reads through the link (exit 0, rooted at `linkocc`, `files` `[]`) both by search and under `--config`, while `--config` naming the directory answers 14.14 as pinned.

Task 33 landed (6f25c5b): T7-2 (`test/suite/registry/section-7-basics.ts`) gained, after its string-literal keys arm, the current entry's remaining arms — the verbatim glob `specs/` + backslash-`u002A` + `.mdx` (the escape built from the backslash's code point as `BACKSLASH`, never spelled in harness source; `build` exit 0, `inventory --json`'s `configuration.specs` carrying the glob as spelled and `sources` `[]`, `ids --json` `files` `[]`), the escape-spelled group name `prod` + backslash-`u0075` + `ct` (a profile `target: "product"` refused through `expectConfigRefused` — which now takes `FileContents` — and the same-spelled target loading with the inventory's group name as spelled and the `verbatim` profile's one leaf uncovered), the encoding arms (`NON_UTF8_CONFIG`: the canonical text plus a trailing line comment holding 0xFF; `BOM_CONFIG`: EF BB BF prefixed; both staged as bytes), `KEY_AND_NAME_VIOLATIONS` (`specs` twice at top level, `product` beside `"product"` inside `specs`, an empty group, profile, and rule name), and the comments arm (`COMMENTED_CONFIG` versus `COMMENT_FREE_CONFIG`: each `build` exit 0, the raw `configuration` member of each form-decoded `inventory --json` compared as value and as serialization); T7-3 gained `REPLACEMENT_NAME_VIOLATIONS` (a string-literal group key, a profile name, and a rule name each holding the raw U+FFFD between two letters). `timeoutMs: 240_000` on T7-2; traceability T7-2 → 7, 2.4, 7.4, 11.6, 14; neither test is in a fixture's in-scope list (CERTIFICATIONS.md's exclusions bullet names these arms), so certification is untouched; the self project stays 360 passed / 1 skipped. Against the built product both tests fail diagnosed at their first new arm — T7-2's inventory reports the glob interpreted (`specs/*.mdx`), T7-3 builds under the U+FFFD-named group — and a scratch per-arm drive shows the rest: the non-UTF-8, both repeated-key, and comment arms pass; the BOM, the three empty names, the `target: "product"` refusal, the group-name inventory (the name reported interpreted as `product`), and the profile and rule U+FFFD names would each fail diagnosed (exit 0 where 2 is pinned; no crash anywhere).

Task 34 landed (fb8436d + 718ca3b): T7-4 (`test/suite/registry/section-7-discovery.ts`) is extended on the current TEST-SPEC entry — the semantics workspace gains the in-segment group `dstar/a**b.mdx` (each `*` the single-segment wildcard: `axxb.mdx` and `ab.mdx` discovered, `a/b.mdx` never); the outside-root arms drive the three pinned spellings `a/../../x/*.mdx`, `**/../x/*.mdx`, `/specs/*.mdx` and the plain `../x/*.mdx`, with `x/M.mdx` staged beside the root (`stageBesideRoot`) so a product deciding by what it finds rather than by spelling discovers it, each `build --json` pinned to exit 2 with the error document's finding `configuration-error` at `xspec.config.ts`, locations [], inside a modifies-nothing compare; the inside-root spellings `a/../b/*.mdx`, `./specs/*.mdx`, `specs//*.mdx`, `specs/*.mdx/`, and — Linux leg — `C:/specs/*.mdx` each run in their own workspace (`expectInsideMatchingNothing`) beside the control group `ctl/*.mdx` over the decoys a normalizing product would match (`specs/A.mdx`, `b/M.mdx`, and `a/N.mdx` making `a/` a real directory), `ids --json` listing exactly the control and `inventory --json` reporting the glob exactly as configured with `sources` exactly the control file under its group; traceability `["7", "14", "11.6"]`. The CONF-DISC conformer (`test/fixtures/conf-disc/product.mjs`) needed two changes: `validatedPattern` now keeps every glob as spelled and applies SPEC 7's depth rule (it had normalized through `path.posix.normalize`, accepting `**/../x` and matching `a/../b`, `./specs`, and `specs//` after normalization), and it gained the `inventory` command in the full 12.7 document form (CERTIFICATIONS.md's CONF-DISC surface names it for exactly this observation: globs as configured, sources with memberships in configuration order, the derived map, an empty record, `outDir` null, empty coverage and policy lists, journal occupancy, a present `.xspec/reviews` refused as a scope error), `discoverSources` exposing its kept matches for the listing; violator outcomes unmoved (VIOL-DISC-DIALECT fails T7-4 alone at the semantics listing, SYMLINK T7-5 alone, DERIVED T7-6 alone). Against the built product T7-4 fails diagnosed at the `**/../x/*.mdx` arm (exit 0 where 14.14 pins exit 2 — the product treats `**` as an ordinary segment when deciding the root), every earlier arm passing, in-segment `a**b.mdx` and `a/../../x` included; driven alone through a temporary export of the helper (AGENTS.md's T14-10 pattern, reverted), the inside-root spellings `a/../b`, `./specs`, and `specs//` fail diagnosed at the `ids` listing (the product normalizes them and discovers the decoy) while `specs/*.mdx/` and `C:/` pass — new diagnosed product failures beside the known ones, no harness error. Self project 360 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 35 landed (d6fdeab): T7.3-1 (`test/suite/registry/section-7.1-7.3.ts`) is extended on the current TEST-SPEC entry — arm (d) now drives one 14.14 arm per pinned non-plain `outDir` spelling (`""`, `"/out"`, `"./out"`, `"out/../x"`, `"out//x"`, `"out/"`; the `INVALID_OUTDIRS` table) beside the two `..`-bearing spellings it always drove (`"../out"`, `"docs/../../out"`), each serialized through `JSON.stringify` so the literal the product reads is the spelling listed; the module's local `expectConfigRefused` now pins the error document form-exact as T7-2's helper does (`code` `configuration-error`, `path` exactly `xspec.config.ts`, `locations` `[]`) and snapshot-compares the workspace around the refused `build` (SPEC 12.1, 12.0), which strengthens arm (b)'s emit-required refusals too; a new arm (d') builds under `outDir: "out/sub"` (`OUTDIR_SUB_CONFIG`) and asserts `out/sub/specs/A.md` and `out/sub/specs/sub/B.md` byte-exact with `specs/A.md`, `specs/sub/B.md`, `out/specs/A.md`, and `out/specs/sub/B.md` vacant. The header's SPEC 7.3 paragraph, its 14.14 bullet, and the title restate the spelling-alone rule (one or more non-empty `/`-separated segments, none `.` or `..`; never where the path would resolve) in place of the old resolves-within-the-root rule. Traceability unchanged (`["7.3", "14"]`); T7.3-1 is in no fixture's in-scope list, so certification is untouched. Green through a scratch stand-in that refuses non-plain spellings with the error document and delegates everything else to the built product (AGENTS.md; a `./xspec.config.ts` path and a stray write before the refusal are each caught at the helper's own assertions); against the built product T7.3-1 fails diagnosed at the `""` arm (exit 0 — the product emits next to each source; it also accepts and normalizes `./out`, `out/../x`, `out//x`, and `out/`, refusing `/out` alone under a resolves-outside-the-root rationale), every earlier arm passing. Self project 360 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 36 landed (19b0a81 + 1b649f1): the configured sets are read as sets and reported in 12.7's value forms. `test/helpers/adapters/forms.ts`'s inventory view decoders now decode a profile's `targetTags` and a selector's `tags` as tag sets (strictly ascending UTF-8 byte order — a repeated or misplaced tag is a form failure, a diagnosed `HarnessAssertionError`) and `edgeKinds`/`kinds` as kind sets (5.2's order, each token once), S-5 gaining a literal positive (`targetTags: ["Z", "a"]`, 0x5a < 0x61, plus proper-subset kind sets) and eight rejection cases; only section-12.7.ts spells another configured set (`["alpha", "beta"]`, canonical), so no other test's failure point moved. T11.6-2 (`test/suite/registry/section-11.6.ts`) dropped `normalizeKindSets` — every list compared literally, `ALL_EDGE_KINDS` in 5.2's order — and gained a fifth workspace spelling `targetTags: ["z", "a", "a"]`, `edgeKinds: ["references", "depends"]`, `kinds: ["embeds", "depends"]`, and a `tags` selector `["b", "a", "b"]`, its whole resolved map pinned with `["a", "z"]`, `["depends", "references"]`, `["depends", "embeds"]`, `["a", "b"]`. T7.4-1 and T7.5-1 (`test/suite/registry/section-7.4-7.5.ts`) gained the set-reading arms: the spelled profile/rule beside its collapsed twin (`setReadingProfileFiles`/`setReadingRuleFiles`, driven by `runSetReadingProfile`/`runSetReadingRule`), each `build` exit 0, `inventory`'s profile/rule pinned literally after the form-exact decode, the coverage rows (`a` covered over depends, `z` uncovered with its embeds edge outside the kinds, the untagged `n` required by neither) and the rule's two 14.12 findings pinned, then the twins' decoded reports compared member for member and their stdout bytes compared. Against the built product all three fail diagnosed at the inventory decode within seconds (the product echoes `targetTags`/`tags` as configured — `["z","a","a"]`, `["b","a","b"]` — and keeps kind lists in configured order, duplicates collapsed), while its `coverage`/`check` answers for the twins are byte-identical; through the AGENTS.md stand-in (inventory's sets rewritten into value forms) all three pass in ~10 s, a perturbed coverage count is caught at T7.4-1's decoded twin compare and a trailing byte at both byte compares. Traceability rows unchanged (12.7 and 11.6 are carriage context, the T11.6-3 precedent); no fixture's in-scope list names the three tests (both sit in CERTIFICATIONS.md's Exclusions), so certification is untouched. Self project 360 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 37 landed (5bcb11d + 05c499e + 603868a): the tag-set datum is compared literally everywhere it is decoded. `test/helpers/adapters/forms.ts` exports `decodeTagSet` (strictly ascending UTF-8 byte order, duplicates collapsed, never case-folded — a repeated, misplaced, or case-folded tag a form failure, a diagnosed `HarnessAssertionError`) and the `view` node's plain-state `tags` decodes through it; `test/helpers/adapters/query.ts` routes the `tags` of `query node`/`show` (`decodeNodeReport`), both summaries, and both row decoders through it too (`expectStringArray` no longer imported there), so no product-facing test can re-sort a tag set after the decode; S-5 gained a positive (`["Zed", "auth"]`) and three rejection cases per node decoder (six decoders). `test/suite/registry/section-2.5-2.6.ts` dropped `sortedTags` — T2.6-1/2/3 compare each decoded array literally — and T2.6-1 gained the reversed spelling `tags="b a"` as a fifth arm (`reversed`), reported exactly `["a", "b"]` through `query node` and `query nodes --tag` with the same metadataHash (SPEC 5.5 hashes the sorted tags); CONF-VALID re-certified green (T2.6-1/T2.6-2 pass on the conformer and both violators). T11.4-3 (`test/suite/registry/section-11.4.ts`) stages the tag-set-form bearers `<S id="bset" tags="b a a">` and `<S id="zset" tags="z A">` in the finding-free `specs/clean.mdx` (`CN_BSET_*`/`CN_ZSET_*`, slice-checked), `CLEAN_TREE` pinning `["a", "b"]` and `["A", "z"]` in both the whole-domain and the `<file>`-operand invocation — on the `view` node alone, as CERTIFICATIONS.md's CONF-AVAIL scope reads the entry (its `query node`/`show --json` mention is the T12.7-1 cross-reference; those surfaces are T2.6-1's and T12.4-1's); the CONF-AVAIL conformer (`test/fixtures/conf-avail/product.mjs`) now orders tag sets with `compareRelBytes` instead of the UTF-16 default `.sort()`; traceability T11.4-3 → 11.4, 2.6, 12.7; the AVAIL family re-certified as pinned (T11.4-3 PASS on the conformer and NOFILE, FAIL on NULLMARKER and OMIT). Against the built product — which echoes a node's tags in spelled order with duplicates collapsed (`["b", "a"]`, `["z", "A"]`) where 12.7 pins byte order — T2.6-1 fails diagnosed at the reversed arm's decode and T11.4-3 at the whole-domain view's decode, and the shared decoders move six further tests onto the same diagnosed failure at their first tag-bearing decode (each a `HarnessAssertionError` naming the spelled array; no harness error): T11-2 (`red blue`), T11-7 (`t2 t1`), T11.4-6 (`tag.α mark`), T6.2-1 and T6.2-2 (`mid keep`), and T5.5-5 (`beta alpha`); P-4 decodes `query node`/`query nodes` over random-order `TAG_POOL` draws, so it fails the same way on the first draw whose spelled order is not byte order (P-13 decodes only the coverage report). The other two `sortedTags` helpers (`test/suite/registry/section-11.ts`, T11-2's row compare and T11-1's node compare; `test/suite/registry/section-12.3-12.5.ts`, T12.4-1's `show`/`query node` projection) are now dead normalizations of an already form-checked datum — the decode fails first on any product that would need them — left for the tasks that own those modules (40–42) or the compliance determination. Self project green under the unprivileged namespace.

Task 38 landed (a47d728): T10.1-6 is registered in `test/suite/registry/section-10.1.ts` (spread through `section101Tests`; traceability "10.1", "10.7", "11.6", "12.2", "13.3", "13.4", "13.5", "14"), in no fixture's in-scope list (CERTIFICATIONS.md Exclusions), so certification is untouched. Its module-local `stageNonDirectoryOccupant` stages a plain file at a path, or relocates the product-written directory there to a sibling inside the root and links its path to it (`workspace.symlink(rel, target, "dir")`), verifying the occupant kind before any product runs (a plain `Error` on misuse); one whole-root `assertLeavesUnchanged` then covers occupant, link, and target together, since the snapshot never follows links. Against the built product it fails diagnosed at the `.xspec/reviews`-symlink arm's `review status s` (exit 0, answered through the link); behind that, `inventory` and `check` read through a symlinked `.xspec` (no 14.23, no unit form) and a plain-file `.xspec` draws no 14.23 from `inventory` — relevant to Task 43 (T11.2-6) and Task 45 (T11.6-1) if they stage the area.

Task 39 landed (0b6be45): T10.7-1 (`test/suite/registry/section-10.7-i.ts`) pins its create-refusal arms on the current TEST-SPEC entry through a module-local `expectCreateRefused` — `review create --strategy audit --name s --json` at exit 1, decoded form-exact as the findings-only report, exactly one finding by counting identity (`assertConditionCounts`), locations `[]`, inside a whole-root `assertLeavesUnchanged` (the snapshot never follows links, so a link occupant and its target are both covered); the finding's concerned path is pinned nowhere (SPEC 10.7, 14.21 — T10.1-6's reading), so it is unasserted. The existing valid session `s` answers `(code-less)` (the plan's task predated the entry's "one code-less finding, nothing modified" clause, which the registered arm checked by exit code alone); four fresh workspaces stage `s` corrupt T10.1-4's way — garbage bytes, a directory, and a symbolic link to the product-written valid session `real` directly (each occupant kind self-checked, a plain `Error` on misuse), and an unknown item status through the H-3 adapter's `stageUnknownItemStatus` over the product-written file — and each `create --name s` answers exactly `14.21 x1`, no code-less refusal beside it. Traceability `["10", "10.1", "10.7", "13.4", "14"]`; T10.7-1 is in no fixture's in-scope list, so certification is untouched. Against the built product the whole test passes in ~5 s — the product reports `corrupt-session` in the refusal's place for all four states, its code-less refusal carrying the informational `identities: ["s"]` — so it stays off the diagnosed-failure list; red-checked through the AGENTS.md wrapper (three modes, recorded there): rewriting the 14.21 finding's code to null or appending a code-less finding beside it is caught at the first corrupt arm's count, a stray `.xspec/reviews/stray.json` beside the refusal at the existing-valid-session arm's compare-around. Self project 360 passed / 1 skipped (19 files) under the unprivileged namespace.

Task 40 landed: the `--file` pattern spellings are shared through `test/suite/registry/support.ts` — `OUTSIDE_ROOT_FILE_PATTERNS` (`../x/*.mdx`, `../x`, `a/../../x`, `/specs/*.mdx`, each with its SPEC 7 depth-rule reason), `BESIDE_ROOT_FILE_PATTERN_DECOY` (`x/M.mdx`, staged beside the root through `stageBesideRoot` before the outside arms — T7-4's discipline, so exit 2 never comes from a side reason; the absolute spelling's premise is the spelling alone), `insideNoMatchFilePatterns(dir)` (`./<dir>/*.mdx` and `<dir>//*.mdx`, sharp only where `dir` holds a discovered `.mdx` at its top level) with `INSIDE_NO_MATCH_FILE_PATTERNS` the pinned `specs` pair, and `expectFilePatternUsageError` (exit 2, the 12.7 error document with `code` null, `path` null, `locations` [] — a plain usage error — and a non-empty stderr). T11-2 (`section-11.ts`) drives the outside table on `query nodes --json` and the inside pair plus its twins over `specs/alpha` (whose normalized form matches A.mdx), each pinned to exactly no rows; T11.3-2 (`section-11.3.ts`) and T11.4-2 (`section-11.4.ts`) add the inside pair to their empty-set tables (sharp over `specs/apple.mdx`/`specs/beta.mdx` and `specs/dup.mdx`/`specs/bad.mdx`, a normalizing product consulting them and exiting 1) and drive the outside table on `occurrences`/`view` (no `--json`: JSON-only surfaces) in place of the former two ad-hoc spellings; T12.3-1 (`section-12.3-12.5.ts`) drives the outside table on `ids --json` and the inside pair plus its twins over `apecs`, each pinned to `files` exactly `[]` (a glob matching no discovered file matches no entry, so the module header's ID-less-entry caveat does not apply). The dead `sortedTags` normalizations in `section-11.ts` and `section-12.3-12.5.ts` are gone (every expected tag list was already in byte order; the decoders enforce 12.7's form). Traceability rows unchanged; none of the four tests is in a fixture's in-scope list (CERTIFICATIONS.md Exclusions), so certification is untouched. Hand-driven, the built product answers every outside spelling on all four surfaces exactly as pinned and normalizes every inside spelling (matching `specs/A.mdx` through `./specs/*.mdx` and `specs//*.mdx` on `query nodes`, `occurrences`, `view`, and `ids` alike); against the built product T11-2 still fails diagnosed at its unfiltered tag-set decode (before the new arms), T11.3-2 and T11.4-2 fail diagnosed at the `./specs/*.mdx` arm (exit 1 where 0 is pinned), and T12.3-1 at the `./apecs/*.mdx` twin (M.mdx listed) after its outside arms pass — three new diagnosed product failures beside the known ones, no harness error. Through the AGENTS.md stand-in (`conform`: the empty form for an inside-nothing value, `query nodes` tags byte-sorted) all four pass whole in ~30 s; a `path` set to the spelling and an empty stderr on an outside arm are each caught at the shared helper's own assertion. Self project 360 passed / 1 skipped (19 files) under the unprivileged namespace.

## Part E — §10–12 arms (after Part B)

## Task 41 — 1.4's alphabet in command values: T11-2 `--tag` syntactic acceptance; T11.3-3 `--to` segments

- **Source:** reviewer B gaps 9 (`--tag` clause), 13.
- **Requirement:** SPEC.md 1.4, 12.0 (a malformed tag or identity spelling is a syntax-class usage error, exit 2, judged before configuration is loaded and before any identity reading); TEST-SPEC.md **T11-2** (§11.1: a well-formed absent tag → exit 0 empty; each malformed spelling — empty, `'a b'`, `#`, `then`, a control character, `"`, `'`, `\`, `&`, U+FFFD — → exit 2 without loading configuration), **T11.3-3** (§11.3: `--to` segments containing `"`, `'`, `\`, `&` — one arm each — and U+FFFD in the path or the id part → exit 2 before identity reading).
- **Files:** `test/suite/registry/section-11.ts` (T11-2), `test/suite/registry/section-11.3.ts` (`TO_MALFORMED` lacks all five).
- **Do:** add the arms; prove "without loading configuration" as T12.0-10 does (the same exit and error document with the configuration invalid or absent).
- **Verify:** both pass or fail as diagnosed.

## Task 42 — T11-4: `--kinds` list grammar

- **Source:** reviewer B gap 10.
- **Requirement:** TEST-SPEC.md **T11-4** (line 411); SPEC.md 11.1, 12.0 (`--kinds depends,`, `,depends`, `depends,,embeds` → exit 2; `depends,depends` reads as `depends` — answers byte-identical).
- **Files:** `test/suite/registry/section-11.ts` (T11-4 ~1152: only `--kinds nonsense` today).
- **Verify:** passes or fails as diagnosed.

## Task 43 — T11.2-6: the obstructing component is itself a discovered file

- **Source:** reviewer B gap 11.
- **Requirement:** TEST-SPEC.md **T11.2-6** (§11.2); SPEC.md 11.2, 13.4, 14.22.
- **Files:** `test/suite/registry/section-11.2.ts` (T11.2-6 stages only the outDir directory replaced by a non-source plain file today).
- **Do:** `outDir: "specs/A.mdx"` (a discovered source): `build` and `check` report condition 22 concerning `specs/A.mdx`; `view specs/A.mdx` is finding-free, exit 0.
- **Verify:** passes or fails as diagnosed.

## Task 44 — T11.4-4: `.xspec` specifier naming a code-group `.mdx`; non-canonical specifier reported canonical (CONF-AVAIL extended)

- **Source:** reviewer B gap 16; reviewer C dependency note (CONF-AVAIL's code-group wrong-kind target and BOM-masked target).
- **Requirement:** TEST-SPEC.md **T11.4-4** (§11.4); SPEC.md 11.4, 2.1, 14.15. In CONF-AVAIL's scope (CERTIFICATIONS.md line 151).
- **Files:** `test/suite/registry/section-11.4.ts` (T11.4-4: the unparseable-target arm exists; add the `.xspec` specifier designating a discovered code source — an `.mdx` matched only by a code group — and the non-canonical `./sub/../BASE.xspec` reporting `specs/BASE.mdx`); `test/fixtures/conf-avail/product.mjs` (extend the conformer to the reworked scope: the code-group wrong-kind target and the BOM-masked target, per the document's CONF-AVAIL scope text, lines 147–151; VIOL-AVAIL-NULLMARKER / OMIT / NOFILE outcomes unchanged).
- **Verify:** `npm run test:self` green (CONF-AVAIL re-certified); passes or fails as diagnosed against the product.

## Task 45 — T11.6-1: physical anchoring through a symlinked working directory

- **Source:** reviewer B gap 18.
- **Requirement:** TEST-SPEC.md **T11.6-1** (§11.6); SPEC.md 11.6, 7 (paths anchored at the physical workspace root).
- **Files:** `test/suite/registry/section-11.6.ts` (T11.6-1, ~411–806: no `symlink(` today).
- **Do:** `R/L → R/a/b` (a symlink inside the workspace): from `R/L`, `inventory` reports the root as `../..` and the configuration as `../../xspec.config.ts`; with a link located above both, `..`. Symlink creation must be an ordinary `fs.symlink` on Linux and macOS; on Windows the arm follows the harness's existing symlink handling (see T13.4-6's staging) — never a skip.
- **Verify:** passes or fails as diagnosed.

## Task 46 — U+FFFD in every argument position and normalization negatives: T12.0-5; U+FFFD arms of T6.4-3 and T6.5-5

- **Source:** reviewer B gap 20; reviewer A gaps 15 (U+FFFD clause) and 18.
- **Requirement:** SPEC.md 12.0 (a U+FFFD anywhere in an argument is a malformed value — syntax class, exit 2, judged before configuration is loaded, error document `code` `null`; `./specs/A.mdx` and `specs//A.mdx` are not normalized — exit 2); TEST-SPEC.md **T12.0-5** (§12.0: the two normalization negatives on `show` and `view`; U+FFFD in each position — node, file operand, `--file`, `--tag`, `--to`, `<new-id>`, session name, `--note`, `--base`, `--config`, `--test-hold`), **T6.4-3** (line 263: a U+FFFD or non-UTF-8 `<new-id>` → exit 2, never the `refused-invalid-id` refusal), **T6.5-5** (§6.5: destinations `specs/B�.mdx` and `specs/B.mdx#x�` → exit 2, `code` `null`).
- **Files:** `test/suite/registry/section-12.0-i.ts` (T12.0-5: four `show` arms today), `test/suite/registry/section-6.4.ts` (T6.4-3 ~1340), `test/suite/registry/section-6.5.ts` (T6.5-5). If T6.4-3's non-UTF-8 leg requires raw argv bytes the subprocess driver cannot pass (Node spawns strings), extend `test/helpers/subprocess.ts` with a Linux-only raw-bytes path (e.g. through `/bin/sh -c` with `$'\xff'` quoting) rather than dropping the leg.
- **Verify:** the three tests pass or fail as diagnosed; each U+FFFD arm proven configuration-independent (invalid or absent configuration, same outcome).
- **Depends on:** Task 6 (T6.4-3's exact-identities expectation).

## Task 47 — T12.0-10: one arm per syntax-class member, configuration-independent

- **Source:** reviewer B gap 21.
- **Requirement:** TEST-SPEC.md **T12.0-10** (line 469) — read whole; SPEC.md 12.0 (the syntax class: `--name=value`, a value-taking flag last, surplus operands, `--status`/`--strategy`/`--coverage bogus`, `--kinds` empty or with a foreign element, two exactly-one-of flags together, `--test-hold` beside `--preview`, a `view` operand beside `--file`, a `.x` session name, `+7`, a malformed `--to`, `--tag a\b`, `--file ../x`, and the rest the entry lists), each identical with the configuration invalid or missing, `code` `null`; plus `query nodes --group` of the wrong kind.
- **Files:** `test/suite/registry/section-12.0-ii.ts` (T12.0-10 stages five arms today; a table of `{argv, expectation}` rows keeps the edit bounded).
- **Verify:** passes or fails as diagnosed.

## Task 48 — Register T12.0-14 (invocation grammar)

- **Source:** reviewer B gap 2.
- **Requirement:** TEST-SPEC.md **T12.0-14** (line 473) — read whole (12.0's token rules, each arm discriminating a lenient parser: `--name=value`, `ids extra`, `ids --file --json`, `--`, `-a` names, `--kinds depends,`, and the remaining arms it lists); SPEC.md 12.0.
- **Files:** `test/suite/registry/section-12.0-ii.ts` or a new `section-12.0-iii.ts` (precedent `-i`/`-ii`), `index.ts`, `traceability.ts` ("12.0", plus each surface's passage as asserted).
- **Verify:** registered (S-1 green); passes or fails as diagnosed.

## Task 49 — Register T12.2-4 (staleness and policy on a failing workspace)

- **Source:** reviewer B gap 3.
- **Requirement:** TEST-SPEC.md **T12.2-4** (line 487) — read whole (14.10 and 14.12 confine themselves on a workspace failing validation as the entry states); SPEC.md 12.2, 13.3, 14.10, 14.12.
- **Files:** `test/suite/registry/section-12.1-12.2.ts` (new entry), `index.ts`, `traceability.ts` ("12.2", "13.3", "14" as asserted).
- **Verify:** registered (S-1 green); passes or fails as diagnosed.

---

End of plan: 49 tasks. When the last task is done, delete this file.
