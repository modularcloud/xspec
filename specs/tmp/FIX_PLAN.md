# FIX_PLAN — Phase 9 (test harness), compliance determination after the 49-task plan (3dd41df, executed through 3265b20)

## Preamble — read before any task

- **Scope.** Phase 9 harness work only: files under `test/`, `.github/`, and harness configuration (`package.json` devDependencies and scripts, `test/vitest.config.ts`, `test/tsconfig.json`). Never modify `src/`. After every task the harness self-tests and the certifications must pass; product-facing tests may fail against the product (diagnosed failures, never harness errors).
- **Known state at 3265b20.** Self project: 360 passed / 1 skipped under `unshare --map-user=1000 --map-group=1000 -- npm run test:self` (the skip is Task 49's subject). The product failures permitted at this phase are diagnosed and recorded in the latest commit messages and in AGENTS.md's stand-in bullets; they need no task here. All three reviewer reports this plan derives from lie verbatim in the shared scratchpad under `compliance-r4/report-{A,B,C}.md` (may be gone in a later spawn; each task below carries what it needs).
- **Environment facts (AGENTS.md is authoritative; consult it before running anything).** The root sandbox needs the unprivileged namespace for the self project (the `unshare` spelling above); the self project and a suite run must not run concurrently (the box is saturated by either); build the product first (`npm run build`) because product-facing tests invoke `dist/cli/bin.js`; single product-facing test: the `-t '<ID> '` filter (trailing space) on the suite project; escape spellings are built from code points in source (never backslash-u literals — the tool-parameter layer decodes them inconsistently, comment text included; this plan writes such characters as `U+XXXX`, and after editing, verify no unintended characters landed); the stand-in wrapper red/green-check pattern; a UTF-8 `TextDecoder` strips a leading BOM unless `ignoreBOM: true`; a wrapper cannot forward non-UTF-8 argv bytes; the S-9 parse recipe (the harness's own devDependencies `micromark`, `micromark-extension-mdxjs`, `mdast-util-from-markdown`, `mdast-util-mdx`; JSX tag matching lives in the mdast layer, so derivability is judged with `fromMarkdown(text, { extensions: [mdxjs()], mdastExtensions: [mdxFromMarkdown()] })`, not the tokenizer alone). Record anything new you learn about building, linting, or running in AGENTS.md — nothing else belongs there.
- **Wiring rules for every task that adds a test ID.** Register the body with `defineProductTest` (`test/helpers/registry.ts`) in a registry module under `test/suite/registry/`; a new module exports a `readonly ProductTestEntry[]`, is spread into `test/suite/registry/index.ts`, and gets a thin wrapper `test/suite/section-<x>.test.ts` calling `declareProductTests` (copy `test/suite/section-6.5-ii.test.ts`); add the ID's passage keys to `test/suite/registry/traceability.ts` (S-1: registry = map; the H-7 key vocabulary is in that file's header, home passage = the TEST-SPEC section number, plus every SPEC passage the entry asserts); run the self project so the S-7 sweep proves the body raises no harness error against the stub, and run the single test against the built product (pass or diagnosed failure).
- **Certification.** In-scope IDs are the explicit lists in `test/self/certification-fixtures.ts`, mirroring CERTIFICATIONS.md (the C-1 gate compares them): CONF-CORE T6.1-2, T10.4-5, T13.4-5, T13.5-1…T13.5-5, T13.5-8; CONF-VALID T1.3-1…T1.3-6, T1.4-1, T1.4-2, T1.4-4, T2.6-1, T2.6-2; CONF-MD T3-1…T3-6, P-2, P-3; CONF-DISC T7-4…T7-6; CONF-AVAIL T11.2-2, T11.2-4, T11.3-4, T11.4-1, T11.4-3, T11.4-4. No test this plan adds is in scope, so certification is touched only by changes to shared helpers/decoders/oracles, to in-scope test bodies (T3-1's restaging, P-2's generator), or to fixture executables under `test/fixtures/`; such a task re-runs the self project in the same iteration and keeps every conformer passing and every violator failing exactly its certified set (the runner prints PASS/FAIL per pair; a violator's FAIL lines on its certified IDs are expected).
- **After Task 3 (the builder-side S-9 check)**: every staging of an MDX source TEST-SPEC declares unparseable (14.20) must be listed in that staging's `mdx.unparseable`; every source relying on an ECMAScript early error the grammar admits (two imports binding one identifier in one ESM block; `export { nope }`; `{1 = 2}`; `{let}`; `{010}`) must name its allowance; `unchecked` is only for sources whose derivability the document does not declare (fuzz mutations, noise files). Every later task staging MDX obeys this.
- **Process.** Commit style `sdg(phase-9): <imperative summary>`; end every commit message with the two trailer lines `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01TyZ5zUv2UCkvTkM1tkYUp2`. Push with `git push -u origin claude/xspec-ui-apis-4df8fa`, retrying on network errors with backoff (2s, 4s, 8s, 16s); check `git log -1` before every push. Never merge or fetch `main`; there is no `gh` and no GitHub API access. One task per engineer iteration, top to bottom (a task's "Depends on" names the only permitted reordering constraint). When a task is done, delete it from this file in the same commit; never discard an undone task; when a task exposes more bounded work than fits one iteration, finish a coherent part and append the remainder as new tasks at the end of this file, precisely described.

## Tasks

### Task 1 — S-9 core: the `deriveMdx` derivability check and its unit self-test
- Cites: TEST-SPEC §17 S-9; SPEC 14.20 (MDX syntax at major version 3, decided by derivability alone); AGENTS.md's parse recipe. Findings C-1 (part), reviewer A's cross-scope note, B-7's shared root.
- Change: create `test/helpers/mdx-derivability.ts` — a pure, product-independent module (no product imports, no I/O, no test-framework dependence) exporting `deriveMdx(source: Uint8Array | string, options?: { allowances?: readonly MdxAllowance[] })` returning a verdict `{ derives: true } | { derives: false; reason: string; position?: { line, column, offset } }`, using the stock MDX 3 parser (`fromMarkdown` from `mdast-util-from-markdown` with `mdxjs()` from `micromark-extension-mdxjs` and `mdxFromMarkdown()` from `mdast-util-mdx`). Byte input is decoded as UTF-8 with `fatal: true` and `ignoreBOM: true`: invalid UTF-8 and a leading BOM are non-derivations (the document declares both 14.20). Export `MDX_ALLOWANCES` as a const list of named allowances — exactly S-9's list: `duplicate-import-binding` (two imports binding one identifier in one ESM block; T2.1-3, T4.5-8), `undefined-export` (`export { nope }`), `invalid-assignment-target` (`{1 = 2}`), `let-as-identifier` (`{let}`), `legacy-octal` (`{010}`) (T14-12). An allowance applies only when the caller names it and only when the parser's rejection is that ECMAScript early error (match the acorn error class/message the parser surfaces); any other rejection, and any rejection under an unnamed allowance, is a non-derivation; an allowance never makes an MDX-syntax rejection pass.
- Self-test: create `test/self/s9-fixture-well-formedness.test.ts` (Tasks 3 and 9 extend it) asserting: derivation for the document's worked well-formed shapes — T3-3's multi-line-tag arms and its U+2028 ESM arm, T6.2-3's worked shape (`foo <S id="m">`, U+000A, `body`, U+000A, two spaces, `</S> bar`), its both-sided U+000B/U+000C spelling and its `body</S>`-with-remainder variant, T6.2-4's pinned shapes, T2.1-6's blank-line-separated ESM block inside a section, T3-7's comment forms, and a representative selection of the composed forms T6.5-13/T6.5-19 name as deriving; non-derivation for declared-unparseable forms — a leading BOM, invalid UTF-8 (`41 E2 82 41`, `C0 80`, `ED A0 80`, `41 E2 82` at EOF), an unclosed `<S id="x">`, a text-position tag with same-line content closed by `</S>` on a later line (T3-1's former shape), `d={}` and `d={ /* c */ }`, `{...a, b}`, `let x = ;` in an ESM block, the one-sided section spellings 6.2/6.5 refuse, T14-12's negative arms; and, for each allowance, that its form derives when named, fails when unnamed, and that a different rejection under a named allowance still fails. Build every non-ASCII/control character from code points.
- Verify: `unshare --map-user=1000 --map-group=1000 -- npm run test:self` green.

### Task 2 — Restage T3-1's and T6.2-3's ill-formed fixtures; re-certify CONF-MD
- Cites: TEST-SPEC T3-1 (asserts `build`/`check` exit 0 and byte-exact emission on a well-formed fixture), T3-3's staging constraint, S-9; T6.2-3; SPEC 14.20. Findings A-15/C-2, A-16 (staging (a) only). Depends on Task 1.
- Change: (1) `test/suite/registry/section-3.ts` `REMOVALS_SOURCE` (~L103) opens `<S id="gamma">Gamma keeps this line.` in text position and closes it with `</S>` at the start of a later line after a fenced block — the stock parser rejects the whole fixture ("Expected a closing tag for `<S>` … before the end of `paragraph`"). Restage `gamma` so it closes within its paragraph, keeping every other arm of the fixture intact, and re-derive `REMOVALS_COMPILED` (~L144) from SPEC 3's line rules. (2) `test/suite/registry/section-6.2.ts` `I3_ROOM_SOURCE` (~L942: `Lead-in prose.<S id="op.imp" …>` closed by `</S>` alone on a later line) is rejected the same way; restage it to 6.2's worked shape — `foo <S id="m">`, U+000A, `body`, U+000A, two spaces, `</S> bar` (with the fixture's own IDs) — and re-derive the arm's expectations (Task 7 adds T6.2-3's further stagings). (3) Add both restaged sources (and their former shapes as non-derivations) to `s9-fixture-well-formedness.test.ts`. (4) T3-1 is in CONF-MD's scope: run the self project; CONF-MD must still pass T3-1 and VIOL-MD-CLASS (T3-3, P-2) / VIOL-MD-CR (T3-4, P-2) must still fail exactly their certified sets; adjust `test/fixtures/conf-md/` only if the restaged shape exposes a conformer defect (the conformer must still conform to SPEC 3 for the new shape).
- Verify: self project green; `-t 'T3-1 '` and `-t 'T6.2-3 '` against the built product run without harness errors.

### Task 3 — S-9 over every deterministic fixture: builder-side declaration and staging-time check
- Cites: S-9 ("runs before any product exists for the deterministic fixtures"), H-8, H-10; SPEC 14.20. Findings C-1 (remainder), B-7 (deterministic side). Depends on Tasks 1–2.
- Change: (1) Extend the workspace builder (`test/helpers/workspace.ts`, the staging entry point every registry body uses) with a per-staging MDX declaration: every staged file whose path ends in `.mdx` is declared well-formed by default and checked with `deriveMdx` at staging time; the staging options gain `mdx?: { unparseable?: readonly string[]; unchecked?: readonly string[]; allowances?: Readonly<Record<string, readonly MdxAllowance[]>> }` (workspace-relative paths). A declared-well-formed source that does not derive, or a declared-unparseable source that derives, throws `HarnessStagingError` (a harness error — never an assertion failure, never a skip) naming the path and the parser's reason. Keep it cheap (in-process parse per file); confirm the stock parser copes with the scale stagings (S-2's 4096-deep tower, T1.3-7's 4,225,030-byte document, S-8's answer-scale documents) — raise the stack in a child process if needed; a scale fixture is never silently exempted. (2) Run the self project: the S-7 sweep now stages every deterministic fixture and every property draw against the stub, so each undeclared unparseable staging surfaces as a harness error. Mark each staging the document declares unparseable (14.20 fixtures in §1.6, §2.4, §2.7, §14, invalid-UTF-8/BOM stagings, T13.x's obstructed sources, …) with `mdx.unparseable`, each early-error form (T2.1-3's and T4.5-8's single-block duplicate bindings) with its allowance, and only fuzz/noise stagings (P-8's mutated documents, non-discovered noise files, S-2's byte-level probes) with `unchecked`. Property generators other than P-8 compose well-formed documents by design: a draw the check rejects is a generator defect to fix, not a source to mark `unchecked`. (3) Any further ill-formed deterministic fixture the sweep exposes: restage it here if small; otherwise append one task per fixture at the end of this plan quoting the parser's reason, and leave that staging un-marked (red) rather than hiding it. (4) Extend `s9-fixture-well-formedness.test.ts` to assert the wiring: a declared-well-formed ill-formed source and a declared-unparseable deriving source each throw `HarnessStagingError` at staging; `unchecked` skips the check; the default is well-formed. (5) Record the declaration option and its rule in AGENTS.md's staging bullet (build/run knowledge only).
- Verify: self project green (S-7 sweep raises no `HarnessStagingError`); a full suite run against the built product (`npm test`, not concurrently with the self project) shows no harness error — only diagnosed product failures.

### Task 4 — Refusal-code vocabulary: `refused-invalid-rewrite` and `refused-moved-import`
- Cites: SPEC 14's refusal-reason list (the two codes appended at e5df8e1); TEST-SPEC T12.7-1, T12.7-2 ("refusal reasons in 14's listed order"), T14-6, T14-7 ("a code 14 does not list never appears"). Finding B-3.
- Change: append the two codes to `REFUSAL_CODE_TOKENS` in `test/helpers/adapters/model.ts` (~L230, currently eight codes ending at `refused-invalid-destination`) in exactly SPEC 14's listed order (read section 14's list to place them); confirm `KNOWN_CODE_TOKENS` in `test/helpers/adapters/forms.ts` (~L252), the token→condition table, the 12.7 findings-order comparator, and every exhaustive switch or list over refusal codes accept and order them; update `section-14.ts`'s T14-7 commentary ("the 25 condition tokens and the eight refusal reasons"); extend S-5 (`test/self/s5-output-adapters.test.ts`) to accept a report carrying each new code and reject an unknown code, updating its pinned rejection count.
- Verify: self project green; certification outcomes unchanged.

### Task 5 — Section-move oracle: the refined `changed` set, multi-line in-line tags, U+000B/U+000C remainders
- Cites: TEST-SPEC §16 P-5 (the oracle's `changed` set now includes "each other node with own-content bytes on a line the deletion joins or drops or the insertion splits"), §17 S-6; SPEC 6.2, 5.6, 3, 1.6. Findings B-10 (oracle part), C-5 (oracle part).
- Change: in `test/helpers/oracles/section-move.ts`: (a) replace the drawing of the `changed` set from "exactly the origin parent, the target parent, and the moved subtree's nodes" (header ~L23–30 and the code) with the refined rule, removing the throw for "any other node whose sequence differs" — an ancestor's or sibling's bytes on a line the deletion joins or drops or the insertion splits is now predicted `changed` by the same own-content comparison; (b) accept the staged multi-line in-line section shapes of T6.2-3 (remove the "multi-line section tag (staged scope)" throw) with agreeing boundary lines (both flow-position or both text-position); (c) handle U+000B/U+000C prose remainders and leads exactly as SPEC 3/1.6 classify them, still delegating each line's keep/drop to the markdown oracle (`compileMarkdown`); (d) re-verify the `metadata-changed`-on-no-node and cascade pins against the refined P-5 text and adjust the header. Extend `test/self/s6-section-move-oracle.test.ts` with vectors for the refined `changed` clause (an ancestor with bytes on the joined line; a sibling's residue on a dropped line; a node whose line the insertion splits) — Task 6 adds S-6's enumerated vectors.
- Verify: self project green; `-t 'P-5 '` runs without harness errors.

### Task 6 — S-6: the section-move oracle's enumerated vector suite
- Cites: TEST-SPEC §17 S-6 (the section-move oracle's fixed vectors), T6.2-3, T6.2-4; SPEC 6.2, 5.6. Finding C-5. Depends on Tasks 1, 5.
- Change: in `test/self/s6-section-move-oracle.test.ts` (today: one worked-case vector `impureRoom` → `impureHall`, the clean-boundary case, T6.2-4 with its contrast) add every vector S-6 enumerates: 6.2's worked straddling-line shape in T6.2-3's three stagings — (a) spaces before its closing tag, (b) the both-sided U+000B/U+000C spelling, (c) the `body</S>` variant with such a remainder — each moved to top level and into a flow-position parent; T6.2-3's clean-boundary case; T6.2-4's pinned final-position shapes and its `changed` twin; T6.2-3's sibling stagings (d) origin `p.s` `changed` and (e) destination `p.s` `changed`. Derive each expected category set by hand from SPEC 6.2/5.6 in the vector (no product involved) and run each vector's composed documents (before and after) through `deriveMdx` inside the test (S-9: the form vectors derive). Fix any oracle defect the vectors expose.
- Verify: self project green.
