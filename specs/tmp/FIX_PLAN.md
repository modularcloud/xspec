# FIX_PLAN — Phase 9 (test harness), compliance determination after the 49-task plan (3dd41df, executed through 3265b20)

## Preamble — read before any task

- **Scope.** Phase 9 harness work only: files under `test/`, `.github/`, and harness configuration (`package.json` devDependencies and scripts, `test/vitest.config.ts`, `test/tsconfig.json`). Never modify `src/`. After every task the harness self-tests and the certifications must pass; product-facing tests may fail against the product (diagnosed failures, never harness errors).
- **Known state at 3265b20.** Self project: 360 passed / 1 skipped under `unshare --map-user=1000 --map-group=1000 -- npm run test:self` (the skip is Task 49's subject). The product failures permitted at this phase are diagnosed and recorded in the latest commit messages and in AGENTS.md's stand-in bullets; they need no task here. All three reviewer reports this plan derives from lie verbatim in the shared scratchpad under `compliance-r4/report-{A,B,C}.md` (may be gone in a later spawn; each task below carries what it needs).
- **Environment facts (AGENTS.md is authoritative; consult it before running anything).** The root sandbox needs the unprivileged namespace for the self project (the `unshare` spelling above); the self project and a suite run must not run concurrently (the box is saturated by either); build the product first (`npm run build`) because product-facing tests invoke `dist/cli/bin.js`; single product-facing test: the `-t '<ID> '` filter (trailing space) on the suite project; escape spellings are built from code points in source (never backslash-u literals — the tool-parameter layer decodes them inconsistently, comment text included; this plan writes such characters as `U+XXXX`, and after editing, verify no unintended characters landed); the stand-in wrapper red/green-check pattern; a UTF-8 `TextDecoder` strips a leading BOM unless `ignoreBOM: true`; a wrapper cannot forward non-UTF-8 argv bytes; the S-9 parse recipe (the harness's own devDependencies `micromark`, `micromark-extension-mdxjs`, `mdast-util-from-markdown`, `mdast-util-mdx`; JSX tag matching lives in the mdast layer, so derivability is judged with `fromMarkdown(text, { extensions: [mdxjs()], mdastExtensions: [mdxFromMarkdown()] })`, not the tokenizer alone). Record anything new you learn about building, linting, or running in AGENTS.md — nothing else belongs there.
- **Wiring rules for every task that adds a test ID.** Register the body with `defineProductTest` (`test/helpers/registry.ts`) in a registry module under `test/suite/registry/`; a new module exports a `readonly ProductTestEntry[]`, is spread into `test/suite/registry/index.ts`, and gets a thin wrapper `test/suite/section-<x>.test.ts` calling `declareProductTests` (copy `test/suite/section-6.5-ii.test.ts`); add the ID's passage keys to `test/suite/registry/traceability.ts` (S-1: registry = map; the H-7 key vocabulary is in that file's header, home passage = the TEST-SPEC section number, plus every SPEC passage the entry asserts); run the self project so the S-7 sweep proves the body raises no harness error against the stub, and run the single test against the built product (pass or diagnosed failure).
- **Certification.** In-scope IDs are the explicit lists in `test/self/certification-fixtures.ts`, mirroring CERTIFICATIONS.md (the C-1 gate compares them): CONF-CORE T6.1-2, T10.4-5, T13.4-5, T13.5-1…T13.5-5, T13.5-8; CONF-VALID T1.3-1…T1.3-6, T1.4-1, T1.4-2, T1.4-4, T2.6-1, T2.6-2; CONF-MD T3-1…T3-6, P-2, P-3; CONF-DISC T7-4…T7-6; CONF-AVAIL T11.2-2, T11.2-4, T11.3-4, T11.4-1, T11.4-3, T11.4-4. No test this plan adds is in scope, so certification is touched only by changes to shared helpers/decoders/oracles, to in-scope test bodies (T3-1's restaging, P-2's generator), or to fixture executables under `test/fixtures/`; such a task re-runs the self project in the same iteration and keeps every conformer passing and every violator failing exactly its certified set (the runner prints PASS/FAIL per pair; a violator's FAIL lines on its certified IDs are expected).
- **After Task 3 (the builder-side S-9 check)**: every staging of an MDX source TEST-SPEC declares unparseable (14.20) must be listed in that staging's `mdx.unparseable`; every source relying on an ECMAScript early error the grammar admits (two imports binding one identifier in one ESM block; `export { nope }`; `{1 = 2}`; `{let}`; `{010}`) must name its allowance; `unchecked` is only for sources whose derivability the document does not declare (fuzz mutations, noise files). Every later task staging MDX obeys this.
- **Process.** Commit style `sdg(phase-9): <imperative summary>`; end every commit message with the two trailer lines `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01TyZ5zUv2UCkvTkM1tkYUp2`. Push with `git push -u origin claude/xspec-ui-apis-4df8fa`, retrying on network errors with backoff (2s, 4s, 8s, 16s); check `git log -1` before every push. Never merge or fetch `main`; there is no `gh` and no GitHub API access. One task per engineer iteration, top to bottom (a task's "Depends on" names the only permitted reordering constraint). When a task is done, delete it from this file in the same commit; never discard an undone task; when a task exposes more bounded work than fits one iteration, finish a coherent part and append the remainder as new tasks at the end of this file, precisely described.

## Tasks

### Task 35 — T6.5-16 `refused-invalid-rewrite`, the finding form and arms (a)–(e)
- Cites: TEST-SPEC T6.5-16 (a section-form move whose exact edits would leave a rewritten file other than well-formed MDX — the origin as its deletion leaves it, the target as its parent rewrite and insertion leave it or as its creation composes it, the one file when origin and target coincide — or a file needing an import with no admissible offset is refused: exit 1, nothing modified (workspace byte-compare, the journal absent or byte-unchanged), exactly one `refused-invalid-rewrite` finding per operation; `locations` the moved section's construct range in the origin plus, for each addition no offset admits, every reference spelling rooted at that binding by its occurrence span; `identities` the concerned files' workspace-relative paths in byte order; `path` `null`); SPEC 6.5, 14, 12.7, 5.7. Finding A-11. Depends on Tasks 4, 31.
- Change: register T6.5-16 in `section-6.5-iii.ts` with the finding-form assertions and the entry's arms (a) through (e) exactly as it states them; each would-be text is verified underivable with `deriveMdx` inside the test (the entry's "verified underivable (S-9)"), each pre-move workspace derives; the preview twins are Task 40's. Leave the body structured for Task 36.
- Verify: `-t 'T6.5-16 '` no harness error; self project green.

### Task 36 — T6.5-16, arms (f)–(i), applicability, and the created-target arm
- Cites: TEST-SPEC T6.5-16 ((f) the paragraph-ended top-level twin, terminated and unterminated — the composed text `para`, U+000A, `<S id="m">x {text(X.a)}</S>`, U+000A derives yet no offset admits the declaration — and its performed control with the flow-form moved text and the `<S id="p">`, U+000A, `x`, U+000A, `</S>`, U+000A target whose only admissible offset is the end of the `</S>` line; (g) no admissible offset, `identities` `["specs/b.mdx"]`; (h) the same-file variant with `identities` `["specs/a.mdx"]` and its control T6.5-13(e); (i) two files concerned, one finding, `identities` exactly `["specs/b.mdx", "specs/z.mdx"]`, `locations` the construct and the embedding's braced container in `specs/z.mdx` in start order; applicability beside `refused-id-collision` and every other applicable reason; the created-target arm whose composed content is underivable (`body</S>` inside the paragraph) — exit 1, nothing created). Finding A-11 (remainder). Depends on Task 35.
- Change: complete T6.5-16 in `section-6.5-iii.ts` with these arms, composed and verified as Task 35's are.
- Verify: `-t 'T6.5-16 '` no harness error; self project green.

### Task 37 — T6.5-17 `refused-moved-import`
- Cites: TEST-SPEC T6.5-17 (a move whose moved text holds an import declaration is refused, judged over the moved text as it stands: exit 1, nothing modified, exactly one `refused-moved-import` finding whose `locations` are each declaration's import range of 11.4 in the origin, `identities` exactly `[]`, `path` `null`; fixture: a valid pre-move workspace whose origin section holds a blank-line-separated ESM block `<S id="m">`, U+000A, U+000A, `import X from "./x.xspec"`, U+000A, U+000A, `body {text(X.a)}`, U+000A, `</S>` — T2.1-6's form — one arm with one declaration, one with two (`import X …`, `import Y …` on successive lines: two locations); an unused-binding arm refused all the same; reported beside every other applicable reason with no intrinsic-validity qualifier — an arm with `<new-id>` `then` reports `refused-invalid-id` and `refused-moved-import` both; the positive counterpart with the declaration in the file's top-level block moving successfully, the moved reference re-rooted through an added binding in the target (T6.5-10), the origin's declaration kept for its remaining use, `check` clean); SPEC 6.5, 14, 11.4, 12.7. Finding A-12. Depends on Tasks 4, 15, 31.
- Change: register T6.5-17 in `section-6.5-iii.ts` per its entry (the preview twin is Task 40's).
- Verify: `-t 'T6.5-17 '` no harness error; self project green.

### Task 38 — T6.5-18: shadow-aware binding choice
- Cites: TEST-SPEC T6.5-18 (fixture: `specs/origin.mdx` holding `x`, `specs/target.mdx` holding `z`, `src/c.ts` = `import O from "../specs/origin.xspec"`, `import T from "../specs/target.xspec"`, a module-scope marker `T.z`, and a function `f` holding `const T = 1` beside the marker `O.x` — valid, compiling clean (H-2); after `move specs/origin.mdx#x specs/target.mdx#y` a declaration binding a fresh `<F>` (value unpinned, never `T`) is added and `O.x` is rewritten through it, `T.z` untouched; the entry's remaining assertions); SPEC 6.5, 4.5. Finding A-13. Depends on Tasks 28, 31.
- Change: register T6.5-18 in `section-6.5-iii.ts` per its entry; assert compile-cleanliness through the TypeScript tooling driver (`test/helpers/tooling.ts`).
- Verify: `-t 'T6.5-18 '` no harness error; self project green.

### Task 39 — T6.5-19: the in-section exclusion
- Cites: TEST-SPEC T6.5-19 (two byte-asserted arms: (a) the target side — result exactly `<S id="p">`, U+000A, U+000A, `x`, U+000A, the moved text, U+000A, `</S>`, U+000A, the declaration, U+000A, the preview's `import-addition` at the file's byte length, the root's own text and ownHash unchanged; (b) the origin side — `<S id="m">`, U+000A, `z`, U+000A, `</S>`, U+000A, `<S id="a" d={"m"}>`, U+000A, U+000A, `y`, U+000A, `</S>` unterminated, `move specs/a.mdx#m specs/b.mdx#m`, result exactly `<S id="a" d={<T>.m}>`, U+000A, U+000A, `y`, U+000A, `</S>`, U+000A, `import <T> from "./b.xspec"`, U+000A; each receiving root's own text and ownHash compared through `query node` before and after; the real operation's bytes agreeing with the preview's offsets, T6.6-4(b); every form deriving, S-9); SPEC 6.5, 6.2, 3. Finding A-14. Depends on Task 33.
- Change: register T6.5-19 in `section-6.5-iii.ts` per its entry, verifying each composed form with `deriveMdx` inside the test.
- Verify: `-t 'T6.5-19 '` no harness error; self project green.

### Task 40 — T6.6-3: the preview twins
- Cites: TEST-SPEC T6.6-3 (extended: preview twins for T6.5-6's refusals, T6.5-16's and T6.5-17's refusals — the same finding, exit 1, `mapping`, `files`, `delta` `null` — the exact self-move of either form, the same-file after-removal collision, and the exit-2 twins over the U+FFFD destinations); SPEC 6.6, 12.7. Finding A-34. Depends on Tasks 36, 37.
- Change: `section-6.6.ts` T6.6-3 iterates only `RENAME_REFUSAL_CASES`, `MOVE_REFUSAL_CASES`, `MOVE_LINK_OUTSIDE_CASES` and the usage tables; add the missing twins, reusing T6.5-16/T6.5-17's stagings through exported constants of `section-6.5-iii.ts` rather than duplicating them, and the exit-2 twins over the exported `MOVE_REPLACEMENT_DESTINATION_CASES` (`section-6.5.ts` ~L3462).
- Verify: `-t 'T6.6-3 '` no harness error; self project green.

### Task 41 — T6.6-4: the pinned tie-break stagings and the full 12.7 comparator
- Cites: TEST-SPEC T6.6-4 (arm (e) pinned at T6.5-13(b) — a self-closing target parent: `import-addition` before `target-insertion`, both zero-length at the tag end; T6.5-13(d) — the EOF paragraph line, terminated and unterminated; the within-class T6.5-13(g) — two `import-addition` entries at one offset, count asserted; the full 12.7 comparator over every edit list); SPEC 6.6, 12.7. Finding A-35. Depends on Task 33.
- Change: `section-6.6.ts` T6.6-4's arm (e) is a generic "coincidence-capable" staging; replace it with the three pinned stagings (reuse `section-6.5-iii.ts`'s exported fixtures) and apply the full 12.7 comparator to every edit list the test decodes.
- Verify: `-t 'T6.6-4 '` no harness error; self project green.

### Task 42 — T14-11: the refined `d`-value ranges; `d={}` as condition 20
- Cites: TEST-SPEC T14-11 (`d={(BASE.a)}` located with its parentheses; `d={BASE.a, BASE.b}` located whole as 14.8; `d={ /* c */ BASE.missing }` with braces, whitespace, comment, and U+00A0/U+FEFF excluded; the spread entry `d={[...BASE.a]}` with `...` included; elisions — one finding per array literal at the whole literal, two literals → two findings; `d={}` and `d={ /* c */ }` as condition 20 at the closing brace); SPEC 14.8, 14.20 ("`d={}` is not well-formed MDX"), 1.7. Finding B-4 (first group). Depends on Task 3.
- Change: `section-14.ts` (T14-11's rules ~L3783–3954): add the missing forms and replace the `d={}` 14.8 pin (~L3801) with the condition-20-at-closing-brace pin, declaring `d={}` / `d={ /* c */ }` stagings `mdx.unparseable` (confirm with `deriveMdx` that the stock parser rejects the empty attribute-value expression; if it derives, record a TEST-SPEC problem per the mission rules instead of pinning).
- Verify: `-t 'T14-11 '` no harness error; self project green.

### Task 43 — T14-11: colliding-declaration located forms, the fragment 14.16 form, UTF-8 offsets
- Cites: TEST-SPEC T14-11 (`let SPEC;` at `SPEC`; `const { SPEC } = o` at `{ SPEC } = o`; `@dec class SPEC {}` from `@`; `export class SPEC {}` from `class`; the fragment `<>`…`</>` form of 14.16; the UTF-8 offsets `41 E2 82 41` → 1, `C0 80` → 0, `ED A0 80` → 0, `41 E2 82` truncated by EOF → 1, in a spec source and a code source alike); SPEC 14.15, 14.16, 14.20, 1.7. Finding B-4 (second group). Depends on Tasks 3, 24 (share the located-form stagings with T4.5-8 through exported constants).
- Change: `section-14.ts`: add the arms; byte-level stagings for the UTF-8 cases (spec sources declared `mdx.unparseable`; code sources are not MDX).
- Verify: `-t 'T14-11 '` no harness error; self project green.

### Task 44 — T14-12 registration, positive arms (new module `section-14-iii.ts`)
- Cites: TEST-SPEC T14-12 (the well-formedness contract: `export { nope }` → one condition-16 finding at the statement, no 14.20/14.15, its import still listed by `view` with its target and the rooted embedding recorded by `occurrences --file`; `{1 = 2}`, `{let}`, `{010}`, `{a, b}`, `d={BASE.a, BASE.b}`, `{await x}`, `{function(){}}`, `{...(a, b)}`, `export const x = <b/>` each well-formed with the findings the entry states; TypeScript post-parse checks leaving a code file well-formed with its marker attributed); SPEC 14.20 (derivability alone), 14.16; S-9's allowance boundary. Finding B-1 (positive half). Depends on Tasks 3, 4.
- Change: create `test/suite/registry/section-14-iii.ts` (export `section14iiiTests`), spread into `index.ts`, wrapper `test/suite/section-14-iii.test.ts`, traceability keys; register T14-12's positive arms, naming the S-9 allowance for each early-error form (`undefined-export`, `invalid-assignment-target`, `let-as-identifier`, `legacy-octal`) and declaring every other form well-formed (the default).
- Verify: `-t 'T14-12 '` no harness error; self project green.

### Task 45 — T14-12 negative arms, masking, the three surfaces; T14-4's stagings
- Cites: TEST-SPEC T14-12 (`010`/`09` in `.ts` at the second digit; `{...a, b}` at its comma; an ESM block holding a `const` statement at the line start; import attributes at `with`; `d={]}` at `]`; `{text(}` at `}`; an unbalanced trailing `{text("a")` at the file's byte length; masking; reported by `build`, `check`, and the 11.2 surfaces); T14-4 ("the 14.16 and 14.20 arms of … T14-12 (all three surfaces)"); SPEC 14.20, 14.16, 11.2. Finding B-1 (negative half). Depends on Task 44.
- Change: complete T14-12 in `section-14-iii.ts` (spec-source stagings declared `mdx.unparseable`; offsets confirmed against `deriveMdx`'s positions) and add T14-4's stagings over T14-12's arms (export the staged sources for T14-4 and Task 46).
- Verify: `-t 'T14-12 '`, `-t 'T14-4 '` no harness error; self project green.

### Task 46 — T14-11: the syntax-failure offsets of T2.3-3, T2.4-2, T2.7-3, T2.7-4, T14-12 "asserted the same way"
- Cites: TEST-SPEC T14-11 (its closing clause over those tests' syntax-failure offsets); SPEC 14.20, 1.7. Finding B-4 (third group). Depends on Tasks 16, 17, 18, 19, 45.
- Change: `section-14.ts` T14-11 gains one arm per such offset, reusing each source through constants exported by its home module (no duplicated staging), asserting the finding's offset exactly as T14-11's other 14.20 arms do.
- Verify: `-t 'T14-11 '` no harness error; self project green.

### Task 47 — T14-7: the refined refusal arms
- Cites: TEST-SPEC T14-7 (rewritten: (a) `refused-invalid-rewrite` — identities the concerned files' paths in byte order, `path` `null` — and `refused-moved-import` — import ranges, `identities` `[]`; (b) identities over invalid paths: `move specs/A.mdx#x 'specs/new.txt#x y'` → `refused-invalid-destination` (`path` `specs/new.txt`) beside `refused-invalid-id` with `identities` exactly `["specs/new.txt#x y"]`; `move specs/A.mdx#x specs/new.txt#p.y` → `refused-invalid-destination` beside `refused-missing-target-parent` with `["specs/new.txt#p"]`; `query node` on such an identity exit 2; (c) the `refused-cycle` sibling arm — A imports a third module C, the moved text carries `d={C.foo}`, C imports B; the finding locates C's existing import of B and the chain spelling in A, a product choosing `C` as the fresh identifier rewriting nothing there — and the located-set rule "spellings rooted at the added binding, independent of whether each appears as a `reference-rewrite`"); SPEC 14, 12.7, 6.5. Finding B-2. Depends on Tasks 4, 36, 37.
- Change: `section-14.ts` T14-7: add (a) by reusing `section-6.5-iii.ts`'s exported stagings, (b) with a `specs/new.txt` fixture (none exists in the registry today), and (c) the `C.foo` staging; the whole-sweep assertion "a code 14 does not list never appears" now covers ten codes.
- Verify: `-t 'T14-7 '` no harness error; self project green.

### Task 48 — CONF-CORE fixture: VIOL-CORE-LATELOCK's acquisition point; `build --json`'s document
- Cites: CERTIFICATIONS.md §VIOL-CORE-LATELOCK (Deviation: "13.5's acquisition point moved past those two checks and no further; the gate and refresh of 13.3, the valid-workspace precondition of `rename`/`move` … still follow acquisition and the hold"; Expected failures: exactly T13.5-8 on its two seam-ordering arms — `rename specs/A.mdx nope x` and `review create --base <ref> --name n` under `--test-hold` — while `review create --strategy audit --name n --test-hold` on the failing workspace and the exclusion-first arm's holding `review resolve` acquire and hold before the gate); SPEC 12.7 (a `build` report's defined content is findings alone: `{"findings": […]}`); CERTIFICATIONS.md preamble and §CONF-CORE Scope (`build` in the command surface, behaving per 12.0). Findings C-3, C-4.
- Change: `test/fixtures/conf-core/product.mjs`: under `lateAcquisition` (~L2187, today the whole `judge(config)` — argument checks, baseline resolution, the 13.3 gate, and the rename/move precondition — runs before `acquire()`) move `acquire()`/`holdIfRequested()` to after the argument checks and baseline resolution and before the gate and the precondition; update `bin-latelock.mjs`'s header and the deviation table (~L3381–3384); `commandBuild` (~L2258–2263) emits `{"findings": []}` (with findings when any, per 12.7) instead of `{ ok: true }`. Re-certify in this task: exactly T13.5-8 fails against VIOL-CORE-LATELOCK, the runner's diagnosis now on the two seam-ordering arms (not on the `review resolve` arm), every other conformer/violator outcome unchanged.
- Verify: self project green with the certification report as stated.

### Task 49 — E-2/H-9: the permanently skipped platform arm
- Cites: TEST-SPEC §0 H-9 (local-only tests are collected in the local suite and never marked skipped), §18 E-2; PROCESS.md's TEST-SPEC requirement (MUST NOT skip tests that cannot run in GitHub CI). Finding C-6.
- Change: `test/self/permission-staging.test.ts` ~L384 `test.runIf(!onLinux)("on a non-Linux platform every staging throws HarnessStagingError at once …")` reports skipped on every Linux run, including the `harness-self` CI job, and no CI leg runs the self project elsewhere. Resolve without a skip marker: run the arm where it can run (e.g. as part of the `windows` project, which the Windows CI leg runs via `npm run test:windows` — the arm then executes there) or make it a local-only test under `test/local/` with its documented reason (E-2). Update `.github/workflows/ci.yml` only if the chosen leg needs it; update AGENTS.md's run notes if a command changes.
- Verify: self project reports 0 skipped; the chosen leg's project lists the arm (`npx vitest list --project <name>`).

### Task 50 — Fixed form-vector sets for the remaining MDX-staging generators (P-1, P-7, P-9, P-12, P-13)
- Cites: TEST-SPEC §17 S-9 (the fixed vector set of the forms the generators compose, checked before any product exists), §16 preamble (each draw checked before the product is driven on it). Follow-up of Task 9, which wired the live per-draw check (`PropertyOptions.mdxSources`, `test/helpers/property.ts`) into every generator staging MDX by construction — P-2/P-3, P-4, both P-5 arms, P-6, P-7, P-9, P-12, P-13 — and enumerated the fixed vector sets S-9 names: `P2_P3_FORM_VECTORS` (`test/suite/registry/section-16-p2-p3.ts`), `P4_FORM_VECTORS` (`section-16-p4.ts`, the PROP-03 rendering P-4/P-5/P-6 share), and `P5_FORM_VECTORS` (`section-16-p5-p6.ts`, the decorated staging and the replay rewrite), each asserted to derive in `test/self/s9-fixture-well-formedness.test.ts`. S-9's letter names only P-2, P-3, and P-5; this is Task 9's own broader rule ("each generator's fixed form-vector set") for the generators it left to the live check alone.
- Change: for each of P-1 (`section-16-p1.ts`: the segment and tag stagings — both quote kinds, the ancestor chain a `.`-bearing draw spells, single line terminators inside a value, the boundary and control representatives; accepted and rejected draws alike derive by construction, per the header's no-blank-line rule, so P-1 also gets `mdxSources` from the pure function that builds its staged source), P-7 (`mdxSection`), P-9 (`renderP9Workspace` over a model exercising every item kind and every edit class's after-state), P-12 (every twist's composed files except the break-parse twist's, which the staging declares unparseable), and P-13 (`renderP13Files` over every rank and reference shape), export a `P<n>_FORM_VECTORS: ReadonlyArray<readonly [name: string, source: string]>` built from the module's own templates and constants (the enumerated forms, not draws — `P2_P3_FORM_VECTORS` is the pattern, contexts included), and add a `describe`/`test.each` group for it to `s9-fixture-well-formedness.test.ts`. A vector that does not derive is a generator defect to fix in the generator (Task 9's precedent: P-2's prose alphabet lost `*`, `_`, `[`, `]`, `(`, `)` because CommonMark emphasis and link pairs straddling an inline tag do not derive), never a vector to drop. P-8 and P-11 stay out (imperfect input by design, 16).
- Verify: self project green (the S-9 file alone runs in ~1 s); each property's `-t '<ID> '` run against the built product unchanged (pass or its diagnosed failure), and P-1's under `XSPEC_PROPERTY_SEED=random` without a harness error.

### Task 51 — Check-side 14.10 set-asides on failing workspaces: retire the stale "not settled by SPEC" tolerance (exposed by Task 27)
- Cites: SPEC 14.10 (on a workspace failing `build`'s validations the mismatch forms — per file and graph data — are undetectable and go unreported, while the unreadable-record form and the recorded-file form are reported whatever the sources' validity), 13.3 (source validation errors, journal errors, and refused writes alike are the validations a workspace fails), 12.2; TEST-SPEC T12.2-2, T14-1, T14-3, T14-4 (its 14.21 row), T11.2-6 (fixture 1). Task 27 pinned `check` exact on the obstructed-write fixtures of T11.2-6 and T13.4-6 by this reading; the governing sentence entered SPEC 14.10 at 204df66 (2026-09-03), after the harness's check-side set-asides were written (2026-08-13/14), so their recorded rationale — "whether prior or missing derived state is detectably stale when the staged defect makes current generation uncomputable is not settled by SPEC 13.3/14" — is stale, and a product reporting phantom mismatch staleness beside the staged family condition passes them.
- Change: `test/suite/registry/section-11.2.ts` T11.2-6 fixture 1 (the garbage-journal `check`: the `nonStale` filter becomes the exact pin `{ "14.13": 1 }`; the module comment's "T12.2-2 set-aside" paragraph and the title's "exact over the non-14.10 findings" clause follow); `test/suite/registry/section-12.1-12.2.ts` `checkFamilyFindings` (T12.2-2's families: the filter becomes exact; the module header's set-aside paragraph follows); `test/suite/registry/section-14.ts` `nonStale` and its consumers (T14-1, T14-3, the T14-4 14.21 row; the header bullet "check-side condition counts set 14.10 staleness findings aside"). Before pinning each staging, judge the two whatever-validity forms: a readable record whose recorded derived paths are all still generated (the pre-built stagings edited to invalid sources, the garbage-journal stagings) → exactly the staged conditions, no 14.10; a staging leaving the record unreadable (14.23) → exactly one 14.10 beside them, the unit form concerning the graph-data area (T10.1-6's `.xspec` arm is the model); a staging removing a source or disabling emission without rebuilding → the recorded-file form, one 14.10 per recorded path no longer generated, concerning it. T14-4's 14.10 row (a passing stale workspace) and T10.1-6 stay as they are.
- Verify: self project green (none of the touched IDs is in a certification scope, so no certification change is expected); `-t 'T12.2-2 '`, `-t 'T14-1 '`, `-t 'T14-3 '`, `-t 'T14-4 '`, and `-t 'T11.2-6 '` each run against the built product without a harness error — diagnosed failures are expected wherever the product reports the mismatch forms on a failing workspace, as it does at Task 27's pins (it reports the deleted emission per file and the absent graph data as the unit form beside 14.22).

## Closing

Forty-nine tasks, plus Task 50 appended by Task 9's iteration and Task 51 by Task 27's. When the last task is deleted, delete this file in the same commit (leaving `specs/tmp/.gitkeep`), so the loop's next compliance determination starts from an empty plan.
