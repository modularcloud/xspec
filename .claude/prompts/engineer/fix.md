# Engineer mission — FIX (workflow engineering mode)

Close exactly the one gap in your spawn prompt — a cited requirement with an observed shortfall, a failing test or check, or an accepted review comment (for review comments: reply on the thread and resolve it when done). Implement the fix, run the relevant tests locally, commit, push.

Report honestly through your structured return, failures included. If the gap traces to a blocking spec defect rather than the implementation, log it per your role rules and set the `problem` field to the problems-file path instead of working around it.
