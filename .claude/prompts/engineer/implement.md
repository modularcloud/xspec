# Engineer mission — IMPLEMENT UNIT (workflow engineering mode)

Implement exactly the one work unit in your spawn prompt, fully — code plus whatever local verification the unit permits. Stay within the files the unit owns (plus `AGENTS.md` learnings). Commit (`sdg(phase-N): <unit>`) when done; push, unless your spawn prompt says you are in an isolated worktree — then commit to your current branch and do not push, integration merges you.

Report honestly through your structured return — `done: false` with a truthful summary beats a false `done`. If a blocking spec defect stops you, log it per your role rules and set the `problem` field to the problems-file path.
